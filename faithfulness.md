# Faithfulness Harness

The faithfulness harness compares the local kernel fragment against observed Lean 4 behavior on small source examples.  It does not make Lean 4 the project specification, because the local specification remains the authority for this repository.  It records agreement and disagreement with Lean 4 as evidence, and every resulting decision still needs a written rule in `spec.md`.

The first corpus lives under `Faithfulness`.  `Accepted.lean` contains Lean source that the installed Lean compiler must accept, including universe-polymorphic definitions, theorem declarations, inductive recursors, proposition-valued recursors, equality elimination, low-level quotients, mutual inductives, nested mutual inductives, indexed structure projections, inherited structures, recursive source definitions, transparent definitions, abbreviations, opaque constants, literal declarations, lifts, dependent pairs, and subtypes.  `ModuleA.lean` and `ModuleB.lean` add a module-boundary import case with a recursor over an imported inductive.  The files under `Faithfulness/Rejected` contain Lean source that the installed Lean compiler must reject, including a non-positive inductive, a theorem whose type is not a proposition, an ambiguous `Sort u` inductive result, forbidden eliminations from propositions into data, invalid projections, failed eta across computed indices, an opacity-sensitive `rfl`, a quotient relation mismatch, and a nested inductive parameter that contains a local variable.

The executable `lake exe leanleanfaith` runs the corpus through the installed `lean` command.  `Accepted.lean` and `ModuleB.lean` must exit successfully, and each rejected file must fail with an expected diagnostic fragment.  The environment variable `LEANLEAN_LEAN` can name a specific Lean binary when the default command does not select the intended compiler.

The local bridge lives in the ordinary regression suite, `lake exe leanleanimport`, and `lake exe leanleandiff`.  The regression suite checks corresponding local expressions for the same behavioral classes, including raw literal translation, natural-literal computation, dependent family parameters, supported sort-polymorphic subsingletons, and recursor generation over declared field types.  The importer smoke test loads selected roots from the accepted corpus, module-boundary roots, and a named core set through Lean's environment, extracts their `ConstantInfo` closures, reads Lean's structure extension for structure roots, and replays the result through the local declaration importer.  The smoke roots now list structures without listing their field projection functions, so the test checks that structure-extension metadata pulls those projection declarations into the closure.  The same smoke file checks that Lean-accepted recursive source definitions and recursive core definitions fail at documented environment-import boundaries.  The differential executable elaborates selected Lean terms, translates Lean's inferred types and reduced values, and checks the local inferred types and normalized values against them.  It also replays named finite environment fragments and compares translated constant types for every imported constant and reduction behavior for imported transparent definitions.  Together these paths include ordinary definitions, opaque definitions, propositions, mutual inductives, nested mutual inductives, inherited structures, projections, natural literals, equality, quotients, `Sigma`, `PSigma`, `PEmpty`, `PUnit`, `Unit`, `ULift`, `PLift`, `Subtype`, `Decidable`, core inductives, and indexed local inductives.  String literals currently have source-level acceptance coverage, but they are absent from the importer smoke roots because the full `String` environment closure reaches recursive string and byte-array definitions outside the specified subset.

`lake exe leanlean-check-module` exposes the environment-closure replay path as a command-line checker for compiled Lean modules.  It takes `--module <module>` plus one or more `--decl <name>` roots, loads the module, extracts the root closure, and replays that closure through the local kernel.  This executable is a practical bridge for checking local Lean programs before the project has a `lean4export` parser, and the independent checker interface remains the later export reader.

The module checker uses the same outcome vocabulary planned for the export checker.  Accepted roots return exit code `0`, unsupported roots return exit code `2`, and local replay rejection after translation returns exit code `1`.  Current realistic examples include `LeanLeanFaithfulness.Accepted.transparentId` and `LeanLeanFaithfulness.Accepted.Pair` as accepted roots, while `LeanLeanFaithfulness.Accepted.natDouble` is unsupported because Lean marks it as a recursive source definition artifact.
