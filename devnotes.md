# Development Notes

This file records sources, rationale, and small plans for the project.  The references below came from the user on 2026-05-02.  They now inform the first specification draft and the initial implementation boundary.  The project remains spec-driven, so the references guide scope and terminology without substituting for the local specification.

## Scope and Rationale

This effort is spec-driven.  It does not derive a Lean kernel in Lean from the Lean 4 sources, and it does not treat the current Lean 4 implementation as a substitute for a specification.  Drafting the specification is therefore a major part of the work, not a preliminary formality.

The first implementation target is a small data fragment.  It includes closed universes, dependent functions, axioms, transparent definitions, and single inductive declarations with strict positivity checking and primitive recursor families.  It excludes the parts of Lean 4 that would force immediate commitments on universe polymorphism, propositions, or richer inductive families before the base recursor story is stable.

## References

| Reference | Status |
| --- | --- |
| <https://arxiv.org/abs/2403.14064> | Reviewed for project contrast.  It describes Lean4Lean as an external checker for Lean written in Lean, and it reports partial kernel correctness work. |
| <https://ammkrn.github.io/type_checking_in_lean4/declarations/inductive.html> | Reviewed for inductive structure.  It treats an inductive declaration as a type, constructors, derived recursors, and recursor rules. |
| <https://github.com/digama0/lean4lean> | Reviewed for contrast in implementation strategy.  The repository describes itself as a Lean 4 kernel or external checker written in Lean 4. |
| <https://github.com/leanprover/lean-kernel-arena> | Reviewed for checker-interface planning.  The harness expects checker executables to read a `lean4export` file through `$IN` and report accepted, rejected, or declined outcomes by exit code. |
| <https://leodemoura.github.io/blog/2026-3-16-who-watches-the-provers/> | Reviewed for the external-checker rationale.  The post frames proof-producing tools as systems that should emit artifacts checkable by smaller, independent watchers. |
| <https://github.com/leanprover/lean4export> tag `v4.29.1`, `format_ndjson.md`, `Export.lean`, and `Main.lean` | Reviewed for the NDJSON artifact format and command-line behavior.  The export checker targets format 3.1.0 as emitted by the Lean 4.29.1 exporter. |
| <https://lean-lang.org/doc/reference/latest/The-Type-System/> | Reviewed for the core language boundary.  The reference manual describes Lean's core terms and the reduction rules used by definitional equality. |
| <https://lean-lang.org/doc/reference/latest/The-Type-System/Inductive-Types/> | Reviewed for recursors, iota reduction, and the well-formedness conditions on inductive declarations. |
| <https://lean-lang.org/doc/reference/latest/The-Type-System/Universes/> | Reviewed for the universe hierarchy and the scope of the omitted universe-polymorphic fragment. |
| <https://lean-lang.org/doc/reference/latest/The-Type-System/Quotients/> | Reviewed for the low-level `Quot` primitives and the `Quot.lift` reduction rule. |
| <https://github.com/nomeata/lean-mini-kernel/tree/master> | Reviewed for comparison.  The repository is a compact Lean-written checker for a Lean export fragment, and its README names deliberate omissions around mutual inductives, nested inductives, proof irrelevance checks, and projection checks. |
| Local Lean 4.29.1 sources, `Lean/Expr.lean`, `Lean/ProjFns.lean`, `Lean/Structure.lean`, and `Lean/Meta/Structure.lean` | Reviewed for projection and structure representation.  Lean has core projection expressions, projection-function metadata for auxiliary declarations, and a persistent structure extension containing direct field order, field projection names, parent projections, and subobject markers. |
| Local Lean 4.29.1 source, `Lean/Declaration.lean` | Reviewed for theorem representation.  Lean records theorem declarations with a stored proof value, and the installed compiler rejects a theorem whose declared type is not a proposition. |
| Local Lean 4.29.1 sources, `Lean/Declaration.lean`, `Lean/Environment.lean`, and `Lean/Replay.lean` | Reviewed for exported declaration shape.  Lean replays axioms, definitions, theorems, opaque definitions, quotient declarations, and inductive declarations into the kernel, while checking generated constructors and recursors against the replay result.  `ReducibilityHints` are kernel metadata on definitions, while opaque declarations remain a separate declaration class. |
| Local Lean 4.29.1 sources, `Lean/Expr.lean`, `Lean/Level.lean`, and `Lean/Declaration.lean` | Reviewed for importer translation.  Lean expressions include binder annotations, metadata, literals, free variables, and metavariables; the local importer erases the semantics-free fields and rejects the forms absent from the local kernel syntax. |
| Local Lean 4.29.1 sources, `Lean/Elab/PreDefinition/Basic.lean`, `Lean/Elab/PreDefinition/Main.lean`, `Lean/Elab/PreDefinition/Structural/Main.lean`, `Lean/Elab/PreDefinition/WF/Main.lean`, and `Lean/Meta/Eqns.lean` | Reviewed for recursive-definition artifacts.  Lean compiles safe recursive definitions into ordinary kernel declarations, records recursive provenance in environment extensions, and uses `mutualDefnDecl` or partial artifacts for unsafe and partial recursion. |
| Local Lean 4.29.1 source, `Lean/Elab/MutualInductive.lean` | Reviewed for inductive universe bounds.  Lean's elaborator checks constructor arguments after the shared inductive parameters against the resulting universe, and it does not impose that bound on the shared parameter binders themselves. |
| Local Lean 4.29.1 source, `Init/Prelude.lean` | Reviewed for kernel-overridden primitive computation and reducible elaborator annotations.  The source marks `Nat.add`, `Nat.mul`, `Nat.pow`, `Nat.sub`, `Nat.beq`, and `Nat.ble` with extern attributes tied to Lean's primitive natural-number implementation, and it records that the kernel and compiler override the logical model for primitive natural-number operations.  It also defines `optParam`, `outParam`, and `semiOutParam` as reducible identity wrappers, and the same source contains additional override candidates that remain outside the admitted fragment until specified. |

## Mathlib Probe Notes

| Date | Probe | Result | Note |
| --- | --- | --- | --- |
| 2026-05-05 | `Mathlib.Logic.Equiv.Defs`, root `Quot.congr_mk` | Accepted | The first run rejected during conversion after checking `Quot.congr`.  Inspection of the exported `Quot.congr_mk` theorem showed that `rfl` needed `Quot.congr` and `Quot.map` to reduce through a partial `Quot.lift`.  The root cause was WHNF spine handling: reducing a function position could expose an applied head, but the checker did not reprocess the rebuilt application spine. |
| 2026-05-05 | `Mathlib.Data.SetLike.Basic`, root `SetLike.ext` | Accepted | The run checked 23 declaration entries and produced environment size 36.  This probe exercises the structure/set boundary after the quotient fix, without exposing a new checker rule. |
| 2026-05-05 | `Mathlib.Data.Quot`, roots `Quotient.lift₂_mk` and `Quotient.out_eq` | Accepted | The roots checked 20 and 26 declaration entries, with environment sizes 33 and 43.  These probes exercise the high-level `Quotient` wrapper without needing a rule beyond the low-level quotient package and ordinary definitions. |
| 2026-05-05 | `Mathlib.Data.Fin.Basic`, roots `Fin.mk_eq_mk` and `Fin.eq_one_of_ne_zero` | Accepted | `Fin.mk_eq_mk` checked 22 declaration entries, while the `lia` proof in `Fin.eq_one_of_ne_zero` checked 1,623 declaration entries and produced environment size 1,859.  This moved the probe into the Omega proof stack without exposing a rule gap. |
| 2026-05-05 | `Mathlib.Data.Finset.Basic`, roots through `Finset.filter_union` | Accepted | `Finset.erase_singleton`, `Finset.range_union_range`, `Finset.filter_singleton`, and `Finset.filter_union` accepted.  The SQLite cache reused 555 declarations and checked 90 new ones for the first `Finset.filter_union` run; after the cache grew, the same root reused all 645 target declarations. |
| 2026-05-05 | `Mathlib.Data.Finset.Basic`, root `Finset.range_filter_eq` | Rejected: performance | Replay reached `_private.Init.Data.List.Nat.Range.0.List.pairwise_lt_range'._proof_1_4` at declaration index 1430, then exceeded the useful probe budget.  Static profiling reports 32,596 expression nodes, 16,094 app nodes, 5,457 transparent-definition constants, and 3,799 transparent-definition head applications for that proof. |
| 2026-05-05 | `Mathlib.Data.Finset.Basic`, roots `Finset.choose_spec` and `Finset.equivToSet` | Accepted | The cache runs checked 34 and 13 new declarations, respectively.  These roots add unique-choice and finite-set-to-set equivalence coverage after the range-filter performance boundary. |
| 2026-05-05 | `Mathlib.Algebra.Group.Basic`, roots `mul_left_comm` and `inv_mul_eq_of_eq_mul` | Accepted | The cache runs checked 13 and 67 new declarations.  These are the first algebraic-structure probes after the finite-set slice. |
| 2026-05-05 | `Mathlib.Algebra.Ring.Basic`, roots `vieta_formula_quadratic` and `noZeroDivisors_tfae` | Accepted | The cache runs checked 169 and 92 new declarations.  These roots exercise ring structure, bundled homomorphism dependencies, and `List.TFAE` proposition packaging. |
| 2026-05-05 | `Mathlib.Algebra.BigOperators.Group.List.Basic`, root `List.prod_range_div` | Accepted | The SQLite-cache run reused 430 declarations and checked 52 new declarations.  This root adds product-folding coverage without crossing a new rule boundary. |
| 2026-05-05 | `Mathlib.Algebra.BigOperators.Group.Finset.Basic`, root `Finset.prod_biUnion` | Rejected: performance | Replay reached `_private.Init.Grind.Ring.Basic.0.Lean.Grind.Ring.intCast_nat_sub._proof_1_2` at declaration index 2615, then exceeded the useful probe budget.  Static profiling reports 196,906 expression nodes, 98,428 app nodes, 35,330 transparent-definition constants, and 25,701 transparent-definition head applications.  A head-frequency scan is dominated by list constructors, casts, heterogeneous addition and subtraction wrappers, and Omega linear-combination certificates. |
| 2026-05-06 | `Mathlib.CategoryTheory.Abelian.Basic`, root `CategoryTheory.Abelian.image_ι_comp_eq_zero` | Accepted with v3 cache | The old SQLite cache format stored rendered declarations as keys and forced bulk environment loading; the shared v2 cache was 2.8 GB and its content keys occupied 2.62 GB.  The v3 cache stores declaration groups by anchor, and `--cache-layer` now streams NDJSON input while replaying each completed declaration.  The abelian artifact reused all 5,174 declaration entries from the migrated v3 cache and accepted with environment size 5,753. |
| 2026-05-06 | `Mathlib.CategoryTheory.Abelian.Basic`, image/coimage and epi/mono batches | Accepted | The image/coimage batch reused all 5,552 target declarations and produced environment size 6,182.  The pullback/pushout batch reused 5,447 declarations, checked 185 new declarations, and produced environment size 6,250.  These runs widen the abelian-category coverage after the cache streaming fix, while keeping the remaining observed failures in the performance class rather than the rule-package class. |

## Small Plan

- [x] Define the minimal scope for the first specification.
- [x] Review the recorded references and the relevant Lean reference sections.
- [x] Draft the initial minimal Lean 4 specification, including inductive types.
- [x] Implement the first running kernel model in Lean 4 for that specification.
- [x] Check that the resulting specification and kernel together constitute a serious proof of concept.

## Next Plan

- [x] Draft the telescope-aware helper-target design note.
- [x] Replace closed helper-target expressions with contextual target schemas and indexed field shapes.
- [x] Rewrite family construction, motive formation, minor-premise generation, and iota reduction around target schemas.
- [x] Check nested positive examples against Lean's local-variable restriction.
- [x] Add focused tests for simultaneous substitution and raw malformed-entry rejection.
- [x] Add generated-declaration validation coverage.
- [x] Record the de Bruijn context and simultaneous-substitution invariants in the specification.
- [x] Introduce shared telescope operations for source-order binding, context conversion, and simultaneous type instantiation.
- [x] Route parameter, helper-local, and field binding through the shared telescope operations.
- [x] Add regression coverage for telescope context order, dependent binding, independent binding, and simultaneous type instantiation.
- [x] Add constructor field dependencies on top of the target-schema representation.
- [x] Add indexed inductive families on top of the target-schema representation.
- [x] Add universe contexts for inference, conversion, axioms, and definitions.
- [x] Extend universe polymorphism to inductive declarations and generated recursors.
- [x] Rebase the data fragment so `Sort 0` is reserved for `Prop` and data inductives live above it.
- [x] Add the known-Prop function-sort rule.
- [x] Add proof irrelevance for terms whose proposition types are equal by conversion.
- [x] Add proposition-valued inductives with Prop-only elimination.
- [x] Add a symbolic `imax` level former.
- [x] Add large elimination for a conservative syntactic subsingleton class.
- [x] Specify the full indexed subsingleton-elimination criterion.
- [x] Make inductive-block admission the primitive path.
- [x] Add mutual and nested mutual recursor tests.
- [x] Replace variant constants with declaration records and primitive metadata.
- [x] Add opaque definitions as checked, non-unfolding declarations.
- [x] Add low-level quotient primitives and the `Quot.lift` reduction rule.
- [x] Add a first Lean 4 faithfulness corpus with accepted and rejected source examples.
- [x] Add core projection expressions and projection declarations for non-indexed one-constructor inductives.
- [x] Add checked theorem declarations that store proof values without unfolding them.
- [x] Add an ordered declaration-script admission path over the checked declaration APIs.
- [x] Add a kernel-style inductive declaration adapter over type-former and constructor types.
- [x] Record reducibility hints on transparent definitions without changing the opaque-declaration rule.
- [x] Add structure metadata for direct fields, parent subobjects, and flattened inherited fields.
- [x] Add exported generated-constructor and generated-recursor replay checks.
- [x] Add dependency-aware replay for declaration collections that are not already ordered.
- [x] Add a first importer from Lean declaration data into local declaration entries.
- [x] Reconstruct declaration entries from finite Lean `ConstantInfo` snapshots.
- [x] Add root-name dependency closure extraction for Lean environments.
- [x] Record and enforce the safe-only trusted replay policy for unsafe Lean declarations.
- [x] Add raw natural and string literal expressions to syntax, typing, normalization, importer translation, and dependency extraction.
- [x] Import Lean's kernel-relevant structure-extension metadata and check field projection metadata during replay.
- [x] Specify recursive-definition import as replay of safe compiled definitions, with rejection for unsafe and partial artifacts.
- [x] Add a first differential term harness comparing Lean-inferred types and reduced values with local checking.
- [x] Expand the faithfulness corpus around literals, lifts, dependent pairs, subtypes, primitive recursors, and module-boundary imports.
- [x] Add differential comparisons for natural literals and selected projection terms.
- [x] List the known Lean 4 divergence boundaries in the specification with reasons.
- [x] Add broad differential replay over named finite Lean environment fragments.
- [x] Add a traceability map from specification clauses to implementation functions and tests.
- [x] Add a metatheoretic account and proof-obligation table.
- [x] Tighten the specification with an explicit grammar, universe-result predicate, and declaration-script entry table.
- [x] Add a paper claim structure and paper-ready boundary table.
- [x] Add typed checker outcomes for accepted, rejected, unsupported, and internal results.
- [x] Add `leanlean-check-module` for root-name closure replay from compiled Lean modules.
- [x] Specify the first accepted `lean4export` fragment.
- [x] Add `leanlean-check-export` as the independent artifact-checking interface.
- [x] Add a first generated export smoke case.
- [x] Expand accepted generated export-checker tests.
- [x] Add generated export-checker rejection tests.
- [x] Add static export-checker rejection tests.
- [x] Add unsupported export-checker tests.
- [x] Add module-checker tests for accepted, unsupported, and rejected outcomes.
- [x] Add an export replay gap report for whole-artifact checker diagnostics.
- [x] Add a module replay gap report for loaded `ConstantInfo` closure diagnostics.

## Current Decisions

The first specification draft lives in `spec.md`.  It now covers the data fragment with universe-polymorphic axioms, transparent and opaque definitions, checked theorem declarations, inductive blocks, parameters, indices, dependent constructor-field telescopes, strictly positive recursive fields, generated recursor families, low-level quotient primitives, core projections, literals, and beta, delta, zeta, iota, projection, natural-literal, and quotient reduction.  It includes `Prop`, proof irrelevance, indexed subsingleton elimination, mutual inductive blocks with shared parameters, structure metadata, reducibility hints, checked declaration replay, a first Lean declaration importer, reconstruction from finite `ConstantInfo` snapshots, structure-extension metadata import for environment closures, safe compiled recursive-definition replay, and root-name dependency closure extraction for Lean environments.  It still omits the higher-level `Quotient` API, source-recursion reconstruction, full string-literal computation, and unsafe declarations.

Universe levels are represented by a small level language rather than by raw natural numbers.  `Sort 0` is now reserved for `Prop`, and the data examples use `Sort 1` as `Type 0`.  Axioms, definitions, and inductive declarations may declare universe parameters, and the inferencer, normalizer, and conversion checker carry an explicit universe context.  Inductive result universes remain explicit, but they may mention the inductive declaration's universe parameters and must stay above `Prop` in the data fragment.  The kernel checks that constructor arguments do not force a larger universe, that constructor targets use the inductive universe parameters, and that generated recursors place the fresh motive universe parameter before the inductive universe parameters, matching Lean's recursor declaration order.  The term language carries explicit universe arguments on constants, because primitive recursors need a motive universe parameter in order to exist as ordinary constants with ordinary types.  The kernel enforces that these universe arguments are arity-correct and closed under the active universe context in both inference and normalization, rather than trusting normalization to see only well-formed constants.

The positivity check is compositional across earlier inductive declarations.  The kernel computes which parameters of each inductive are positive, and it uses that information when another inductive nests a recursive occurrence under that type constructor.  The recursor generator follows the same structure, producing helper recursors for nested positive targets such as `List T` in addition to the primary recursor for `T`.  Recursor constants now type-check through the ordinary spine rule, and iota reduction is confined to saturated applications.

The inductive checker now analyzes canonicalized field types rather than surface syntax.  It normalizes constructor field types and specialized nested targets before testing parameter positivity, before classifying recursive field shapes, and before deduplicating helper-recursion targets.  That change removes three integrity problems from the earlier draft: local `let` bindings no longer change whether a parameter counts as positive, nested arguments no longer fail positivity because of reducible syntax inside a non-positive parameter, and definitionally equal nested targets no longer generate duplicate helper recursors.

The helper-target redesign is now in place.  Nested helper targets are represented by canonical target schemas with local telescopes, then interned into indexed family targets before motive formation or reduction.  Lean rejects nested inductive parameters that contain local variables, so the local positivity check now rejects fields such as `(n : Nat) → WrapAt n T` while still supporting ordinary function-valued recursive fields such as `(n : Nat) → List T`.  The representation removes the last dependence on lowering nested targets into closed expressions.

Primitive recursor reduction now checks that the constructor universe arguments and target arguments are definitionally equal to the instantiated schema before applying an iota step.  That side condition matters because normalization is available on raw expressions, not only on terms that have already passed inference.  Helper-recursion targets store their own target universe levels, so a nested helper recursor through a polymorphic inductive can construct minor-premise targets at the same levels that appeared in the nested field.

The target-schema rewrite required one further correction in the term language itself.  Open substitution across a schema context cannot be implemented as repeated one-variable substitution, because later substitutions then rewrite variables inside earlier inserted terms.  The kernel now uses simultaneous instantiation for `Expr.instantiateMany`, which restores the intended behavior for parameterized recursors such as `WrapAt.rec` and for helper targets that depend on inductive parameters.

Generated declarations now undergo kernel-side validation before admission to the environment.  The earlier draft built constructor and recursor types, then trusted them.  The kernel now rechecks generated constructor types against the extended environment and rechecks generated recursor types against the inductive and constructor declarations they mention.  That validation step exposed a second structural bug in the inferencer: context lookup must lift a stored binder type back into the current context before returning it.  The kernel now does that, which restores dependent typing for generated recursor binders.

The regression suite now has a separate `leanleantest` executable.  It covers simultaneous substitution directly, including nonzero cutoff and lifting of open inserted values.  It also checks that generated-declaration validation rejects an ill-typed generated type, and that raw inference and normalization entry points reject malformed primitive uses.  The demonstration executable remains available as `leanlean`.

The kernel now has a shared telescope core.  Source-level telescopes remain outermost first, runtime contexts remain innermost first, and the checked code routes conversion between those orders through one API.  The API separates dependent binding, independent binding, and simultaneous type instantiation, because later constructor-field dependencies must not inherit the independent-field assumptions used by the current constructor type generator.  The current `Telescope` type is an abbreviation for `List Binder`, so code review still has to reject direct list operations that recreate binder arithmetic; a wrapper remains an option if that discipline proves too weak.

Constructor fields now form dependent telescopes.  The checker validates each field under the parameters and earlier fields, constructor types bind fields dependently, and recursor minor premises bind all constructor fields before binding the induction hypotheses in field order.  This order matches Lean's exported recursor types for nested helpers such as specialized `List.cons` cases.  Helper-target construction trims unused trailing locals before interning a target schema; without that rule, a target such as `List Tree` encountered after a previous field would generate an infinite sequence of helper targets with irrelevant field locals.

Indexed inductive families now use explicit constructor result targets.  The primary recursor's motive quantifies over the indices before the target term, while constructor minor premises compute their motive arguments from each constructor's declared result target.  Root indices are target locals for the final recursor application but not uniform minor-premise locals; helper targets still bind their schema locals uniformly.  The examples now cover Type-valued equality, vectors, a recursive height-indexed tree, and rejection of a recursor call whose target index disagrees with the constructor result.

Universe-polymorphic definitions and inductives now have checked paths through the kernel.  `addDefinitionWithLevels` and `addAxiomWithLevels` reject duplicate universe parameters and reject level variables outside the declared universe context, while `addInductive` applies the same discipline to inductive result universes, constructor types, and generated recursor types.  The raw public entry points still reject open universe levels.  The examples `polyId.{u}`, `PolyBox.{u}`, and `Eq.{u}` instantiate above `Type 0`, with `PolyBox` using Lean's `Type u` convention as `Sort (u + 1)` and `Eq` ranging over arbitrary `Sort u`.  The focused regression tests cover symbolic level ordering, universe-context closure, generated recursor level ordering, rejection of proposition-valued data inductives, the known-Prop function-sort rule, and iota rejection for constructor targets at mismatched universe levels.

The Prop work now covers the basic sort, conversion, and inductive boundaries.  Axioms may introduce propositions and proofs, and `∀ x : A, B` lives in `Sort (imax u v)` when the domain has sort `u` and the codomain has sort `v`.  The `imax` normalizer reduces known-Prop codomains to `Prop`, reduces known-data codomains to `max`, and preserves unresolved symbolic `imax` terms instead of approximating them.  Level equality also identifies identical symbolic arguments such as `imax u u` with `u`.  Conversion is context-aware internally, and proof irrelevance treats two terms as equal when their inferred proposition types are equal by conversion.  Proposition-valued inductives do not use the data-constructor universe bound, and equality now lives in `Prop` with Lean's parameter/index split: the left side is a recursor parameter and the right side is the index.  Proposition-valued inductives generate large-eliminating recursors exactly when they have no constructors, or one constructor whose fields are propositions or values forced by whole target indices.  The previous approximation accepted a data field whose type was an inductive parameter even when the field value was not forced by the target; the `DataWitnessProp` regression test now rejects that unsound extraction pattern.  The `IndexSingleton` test accepts the exact indexed case, and `ShiftedIndexProp` rejects a field that appears only inside a computed index.

Inductive admission is now block-first.  `addInductiveBlock` creates the provisional environment for every block member before target checking, data-universe checks, positivity, recursor construction, and generated-declaration validation; `addInductive` is a singleton wrapper.  The current block rule requires all members to share the block universe parameters and parameter telescope.  Positive-parameter facts are computed as a joint fixed point over the block, and recursive-shape analysis treats every block member as an open recursive target.  A mutual block now has one shared recursor-family record: block-member targets use their own recursor names, and nested helper targets use the first block member's indexed recursor names, matching Lean's exported declarations.  The tests cover ordinary mutual recursion, nested mutual recursion through `List`, correct helper names, and rejection of a negative mutual occurrence.

Environment entries now use one declaration record with explicit kind metadata.  A declaration records its name, universe parameters, type, optional checked value, kind, and reducibility hint for transparent definitions; primitive recursors are represented as primitive metadata instead of a separate constant variant.  Transparent definitions unfold through `ConstantInfo.value?`, while opaque declarations and theorem declarations keep their checked values but return no reduction value.  Reducibility hints are stored as definition metadata, matching Lean's exported declaration shape, and the current normalizer still unfolds every transparent definition.  Theorem admission checks that the declared type lives in `Prop` and that the supplied proof has that type.  Ordered declaration scripts now fold over the same checked APIs used by direct admission, so a later export parser can construct a script without becoming part of the trusted typing boundary.  Kernel-style inductive declarations can now enter as level parameters, a parameter count, type-former types, and constructor types; the adapter reconstructs the local source-shaped block and then uses the same inductive checker.  This separates declaration admission from primitive reduction behavior, which is the representation needed for theorem, projection, quotient, and future exported declarations.

Export replay now has checks for generated constructors and recursors.  These checks do not add declarations; they compare exported constructor and recursor entries against the declarations generated by the earlier inductive admission.  The comparison uses alpha-equivalence for types, because the local generator may choose binder names that differ from an exported artifact while preserving the typed declaration.  Lean-imported recursor entries also carry the exported `RecursorVal` metadata, and replay checks the block-member list, parameter count, index count, motive count, minor count, rule constructor field counts, and rule right-hand sides.  The right-hand-side comparison synthesizes the expected rule from the same recursor family that drives local iota reduction, then alpha-renames exported universe parameters before comparing expressions.

Dependency-aware replay now computes the constants mentioned by declaration entries and admits entries once their external dependencies are present.  The rule allows a declaration to refer to names it defines itself, which covers mutual inductive blocks and constructor targets.  It rejects a collection when a pass makes no progress, rather than trying to repair missing dependencies with axioms or placeholders.

The first importer from Lean kernel data now translates Lean `Declaration` values into local declaration entries.  Safe axioms, safe definitions, theorem declarations, safe opaque definitions, quotient declarations, and inductive declarations become ordinary script entries, while generated constructor and recursor `ConstantInfo` values become replay checks against regenerated local declarations.  The importer erases binder annotations, expression metadata, recursive-definition provenance metadata, and the `let` nondependence flag, following the Lean source comments that those fields do not affect kernel typing or conversion.  It translates natural and string literals.  It rejects level metavariables, free variables, term metavariables, unsafe declarations, partial definitions, mutual-definition declarations, and unsupported sort-polymorphic inductive results because the local kernel has no rule for admitting those objects.

Trusted Lean replay is safe-only.  The importer rejects unsafe flags on ordinary declarations, inductive declarations, generated constructors, and generated recursors before any declaration reaches local replay.  Partial definitions remain rejected for the separate reason that partial recursion does not have a local kernel-facing rule.  Recursive definitions marked by Lean's recursive-definition extension are accepted as ordinary definitions only when their compiled values and dependency closures recheck locally.  Local declaration scripts still exist as kernel-facing inputs, but Lean import data must pass through the safe-only boundary.

Literal support follows Lean's raw `Expr.lit` boundary.  Natural literals infer type `Nat` only when the environment also contains the constructors used by constructor-view computation.  Weak-head reduction and full normalization now keep raw natural literals compact, while conversion compares their numeric value with either another literal or a `Nat.zero`/`Nat.succ` spine.  `Nat.rec` reduces on literal targets by viewing `0` as `Nat.zero` and `n + 1` as `Nat.succ n` for one iota step, with the predecessor retained as a raw literal.  String literals infer type `String`, but they remain neutral in this subset because Lean's full string reduction goes through `String.ofList` and character-list data that the local kernel has not yet specified.  The Lean importer now translates natural and string literals and records their type constants during dependency extraction, because Lean's ordinary constant-collection path does not expose those implicit dependencies.

The importer can now translate finite `ConstantInfo` snapshots.  It groups inductive type formers by their `all` field, requires every group member and every listed constructor record to be present, checks that the grouped members share universe parameters and parameter counts, and then emits a kernel-style inductive declaration plus generated constructor and recursor replay checks.  The snapshot path rejects incomplete groups instead of inventing missing constructor declarations from names alone.

The importer also has a root-name closure extractor for Lean environments.  The extractor follows constants mentioned in declaration types, definition or theorem values, opaque values, inductive `all` and constructor lists, constructor parent metadata, recursor metadata, literal types, and Lean's structure extension.  Structure roots add their direct field projection functions, parent projection functions, parent structures, and subobject structures to the closure before replay.  This step separates dependency discovery from declaration admission; the final gate remains the local replay checker.

The first real-environment smoke test imports roots from the accepted faithfulness corpus and replays the extracted closure locally.  It exposed two recursor-order mismatches.  Lean places the motive universe parameter before inductive universe parameters in generated recursors, and mutual recursors keep motives and minor premises in block order for every root rather than rotating the eliminated type to the front.  The local generator now follows those rules, and generated-declaration replay compares universe parameter names by alpha-renaming before comparing types.

The widened smoke test now imports ordinary definitions, safe compiled recursive definitions, opaque definitions, propositions, mutual inductives, nested mutual inductives, dependent structures with projections, equality, quotients, `Nat`, `Bool`, `List`, `True`, `False`, `And`, `Or`, `Exists`, `Subtype`, `Sigma`, `Prod`, `PEmpty`, `PUnit`, `Unit`, `Empty`, `Option`, `ULift`, `PLift`, `PSigma`, `Decidable`, and indexed local inductives from real Lean environment data.  It now checks recursive source definitions from the corpus and recursive core definitions such as `Nat.add` and `List.map` by replaying their compiled closures.  The nested mutual mismatch came from two local rules that did not match Lean's exported recursors: the local generator named mutual targets as root-relative helper recursors, and it interleaved induction hypotheses immediately after recursive fields.  The generator now uses a shared block recursor family, gives block members their own recursor names, gives nested helpers names under the first block member, and binds all constructor fields before induction hypotheses in each minor premise.

The source corpus now contains declarations for natural and string literals, `ULift`, `PLift`, `Sigma`, `Subtype`, and a two-module boundary example.  The importer smoke roots include the natural literal, lift declarations, dependent pair, and subtype value.  The string literal remains source-level corpus coverage only: importing the `String` root through the current environment closure reaches recursive string and byte-array machinery that the local specification still rejects.

Broad core replay exposed two additional rule errors.  First, the local data-universe check was bounding the shared inductive parameter binders, which rejected Lean's `Sigma` and `PSigma`; Lean checks constructor arguments after `numParams`, so the local rule now follows that boundary.  Second, recursor generation used normalized constructor field types when forming minor-premise binders, which made `Decidable.rec` use the unfolded body of `Not p`; the generator now normalizes for positivity and helper-target discovery while preserving the declared field binders in generated declarations.

Sort-polymorphic inductive support is deliberately narrow.  The local kernel now accepts empty sort-polymorphic inductives and one-constructor no-field sort-polymorphic inductives, matching the prelude shapes of `PEmpty` and `PUnit`.  It still rejects sort-polymorphic inductives that can carry data, preserving the Lean rejection represented by the `BadPolyBox` corpus case.

The quotient primitive subset follows the Lean reference's low-level `Quot` API rather than the derived setoid-based `Quotient` interface.  The local syntax makes the underlying type and relation explicit in `Quot`, `Quot.mk`, `Quot.lift`, `Quot.ind`, and `Quot.sound`.  Primitive declaration validation checks all five types before admission.  Reduction contains one quotient rule: a saturated `Quot.lift` whose target reduces to `Quot.mk` computes to the representative function applied to the representative, after the reducer checks universe, type, and relation agreement.

The first faithfulness harness now lives under `Faithfulness`.  It runs small Lean source files through the installed Lean compiler and separates examples that Lean must accept from examples that Lean must reject.  The ordinary kernel regression suite contains matching local bridge tests for the same behavior classes, because the project does not yet translate Lean source or exported declarations into the local syntax.

The first differential term harness lives in `Faithfulness/Differential.lean`.  It elaborates selected Lean terms inside Lean, asks Lean for each inferred type and reduced value, translates the term, type, and value into the local syntax, replays the required Lean environment closure locally, and checks the local inferred type and normalized value against Lean's results.  The initial cases cover transparent definitions, abbreviations, natural literals, primitive recursors, large proposition recursors, equality recursors, sort-polymorphic unit recursors, `Sigma` and `Subtype` projections, dependent projections, and `Decidable.rec`.  The same executable now replays named finite environment fragments and compares the translated type of every imported constant, plus reduction behavior for every imported transparent definition.

Projection work follows Lean's representation split.  The core term language now has `proj S i s`, because treating projections as ordinary constants would hide a kernel reduction rule behind declaration names.  Projection functions are represented in the environment, with metadata recording the structure name, constructor name, number of parameters, projection index, and constructor-field index.  The implementation covers one-constructor inductives, dependent field types through earlier projections, projection functions as checked declarations, Prop projection rejection for computational fields, indexed projection maps that skip whole-index fields, and eta for non-recursive data structures whose constructor target matches the major premise type.

Structure metadata follows Lean's split between kernel declarations and elaborator structure information.  A structure record in the local environment records direct fields, projection names, direct parents, and subobject fields, while parent projections remain ordinary checked projection declarations.  Flattened inherited-field lookup is metadata over those declarations; it does not add a reduction rule beyond the existing projection and delta rules.

Lean's structure extension is now imported for the fields that affect kernel-facing replay.  The importer reads `StructureInfo.fieldNames`, `fieldInfo.projFn`, `fieldInfo.subobject?`, and `parentInfo`, then validates each direct field projection against Lean's `ProjectionFunctionInfo`: constructor name, parameter count, and field index must match the structure declaration.  The local structure registration repeats the relevant check over translated declarations by requiring each direct field projection to be either a local projection declaration or a checked lambda whose body is the corresponding core projection.  Parent projections must also have stored checked values, and their types must return the declared parent structure after binding the child parameters and child value.  Binder annotations, deprecated auto-parameter fields, defaults, resolution order, class-instance metadata, and notation data stay outside the trusted kernel subset because they guide elaboration rather than typing or conversion.

The first external checker interface was `leanlean-check-module`.  It is a module-loader bridge over the existing root-name closure importer, and it remains useful because it can compare local replay against Lean's loaded environment.  It returns typed outcomes: accepted roots return `0`, local replay rejections after translation return `1`, unsupported Lean artifacts return `2`, and internal checker failures return `3`.  The executable initializes Lean's search path from the current Lake build directory and loads environment extensions so that structure metadata and projection metadata are available during closure extraction.

The module-checker tests now cover all three user-facing non-internal outcomes.  `LeanLeanFaithfulness.Accepted.transparentId` accepts, `LeanLeanFaithfulness.UnsupportedModule.unsafeId` returns unsupported because Lean records it as an unsafe definition, and `LeanLean.Export.checkString` returns rejected after local replay reaches `Nat.Linear.Poly.denote_reverse`.  A simple source-level `partial def` in the same unsupported test module appears in Lean 4.29.1's loaded environment as a safe opaque constant, so the module checker treats that root as an ordinary opaque declaration.  Explicit partial records in `lean4export` input remain unsupported.

The module checker now has a diagnostic `--gap-report` mode.  It collects the loaded `ConstantInfo` closure without applying the ordinary trusted-replay rejection gate, reports unsafe and partial constants as replay-policy skips, and translates the remaining safe non-partial constants through the ordinary local declaration path.  This mode follows Lean replay's skip policy for diagnostics while leaving the normal module checker stricter: an unsafe root still returns unsupported outside gap-report mode.  The module-checker test script covers both an accepted root and an unsafe skipped root.

The module checker can check selected roots from LeanLean's own compiled modules.  The accepted roots currently include `LeanLean.Level` with 367 declaration entries, `LeanLean.Literal` with 363 entries, `LeanLean.Expr` with 382 entries, `LeanLean.Binder` with 388 entries, `LeanLean.Level.defEq` with 375 entries, `LeanLean.checkDefEqIn` with 556 entries, and `LeanLean.replayDeclarations` with 617 entries.  The runs found an importer bug around Lean's representation of `Quot.sound`: Lean stores `Quot`, `Quot.mk`, `Quot.lift`, and `Quot.ind` as quotient records, but stores `Quot.sound` as an axiom, so the snapshot importer must add quotient primitives once and then check the exported `Quot.sound` type against the primitive declaration.

The `leanlean-self-check` executable now checks a named kernel-only slice without asking the user to enumerate roots.  The default roots are `LeanLean.Name`, `LeanLean.Level`, `LeanLean.Literal`, `LeanLean.Expr`, and `LeanLean.Level.defEq` from `LeanLean.Syntax`, plus the local aliases, `LeanLean.Binder`, `LeanLean.checkDefEqIn`, and `LeanLean.replayDeclarations` from `LeanLean.Kernel`.  The default run checks 181 declaration entries from the syntax module and 388 declaration entries from the kernel module.  The executable augments checked structure roots with their projection declarations before structure metadata is validated, because field projections cannot be treated as axioms when the structure record itself is under check.

The small host-list indexing helper is local structural recursion in both `LeanLean.Syntax` and `LeanLean.Kernel`.  An earlier revision delegated to Lean's `List.get?Internal`, but that moved the self-check through sparse-case helpers and bit operations unrelated to the kernel code under review.  The recursive helper is now checked through the same compiled-recursion path as the rest of the local source-facing declarations.

The self-check trusted base keeps definition values only for named transparent constants whose computation is part of the current boundary: `optParam`, `outParam`, `semiOutParam`, `Unit`, `DecidableEq`, `OfNat.ofNat`, `instOfNatNat`, and the admitted natural-number primitive heads.  `OfNat.ofNat` and `instOfNatNat` are ordinary transparent prelude definitions that Lean inserts for numeric literals; keeping their values lets generated declarations compare `OfNat.ofNat Nat 0 (instOfNatNat 0)` with `Nat.zero` through ordinary delta and projection reduction.  `Unit` is kept for the same reason: checking broader kernel roots can require ordinary delta reduction from `Unit` to `PUnit.{1}`.  Other non-root definitions remain type assumptions, which prevents the checker from silently unfolding arbitrary external library code while checking local roots.  The self-check path also rejects unsafe and partial dependencies before translating them into local assumptions.

The explicit `--module-closure` mode now accepts the source-facing roots from `LeanLean.Syntax` and `LeanLean.Kernel`.  The syntax module checks 430 declaration entries and skips 466 non-kernel-facing roots; the kernel module checks 961 declaration entries and skips 1297 non-kernel-facing roots.  The skipped roots include generated match helpers, no-confusion helpers, aux eliminators other than the recursive `below` and `brecOn` path, sparse-case helpers, derived instance support, and generated representation support.

The self-check classification separates recursive aux declarations from other generated support.  Recursive `below` and `brecOn` declarations are replayed when they occur in a checked closure, including the associated `.go` and `.eq` support names that Lean does not mark as aux recursors.  Other generated support declarations enter as typed dependencies unless the user selects them as explicit roots, because the current self-check claim concerns source-facing kernel code rather than Lean's generated support library.

The export-backed self-check script lives at `tools/export-self-check.sh`.  It exports the same named source-facing roots used by the default self-check and then checks those NDJSON artifacts with `leanlean-check-export`.  The current selected-root artifacts accept: `LeanLean.Syntax` checks 353 declaration entries, and `LeanLean.Kernel` checks 510 declaration entries.  These counts differ from module-loader self-check counts because `lean4export` emits artifact closures and the export checker does not use the self-check trusted-base assumption policy.

The full export-backed self-check script lives at `tools/export-full-self-check.sh`.  It asks `leanlean-export-roots --self-check` for source-facing declarations plus recursive `below` and `brecOn` support, exports those roots with `lean4export`, and checks them with `leanlean-check-export --self-check-roots`.  This rooted export mode uses the same boundary as the module-loader self-check: selected roots and recursive aux support are checked from the artifact, non-root definitions and theorem proofs enter an explicit trusted base, and only the named transparent trusted-base definitions above keep values for conversion.  The same script now runs `leanlean-check-export --gap-report --self-check-roots` so the rooted assumption boundary appears as checked, assumed, and trusted generated summaries in the test output.  The current full run accepts: `LeanLean.Syntax` checks 192 declaration entries, assumes 1523 trusted-base entries, and trusts 225 non-root generated or primitive comparison checks; `LeanLean.Kernel` checks 724 declaration entries, assumes 2089 trusted-base entries, and trusts 245 non-root generated or primitive comparison checks.  The kernel run includes the syntax self-check roots because kernel declarations rely on checked syntax abbreviations such as `LeanLean.Name`.

The generated-support inventory is available as `lake exe leanlean-self-check --generated-inventory`.  The first run inventories 466 skipped declarations from `LeanLean.Syntax` and 1297 from `LeanLean.Kernel`.  Some skipped classes already replay when selected as roots, including many match helpers, recursive aux definitions, private aux definitions, no-confusion definitions, representation support definitions, derived inhabited definitions, and generated proof theorems.  Other classes currently reject or remain unsupported, including no-confusion definitions, constructor eliminators, sparse-case helpers, theorem proof artifacts, derived decidable support, unsafe-recursive support, and aux recursors.  The inventory keeps those facts visible without changing the default source-facing self-check claim.

The generated-support self-check mode is available as `lake exe leanlean-self-check --generated-support`.  It uses the source-facing roots as the baseline, adds every generated-support declaration that replays when selected as a root, replays that combined closure, and reports additional typed assumptions.  The current combined generated-support replay accepts for both local modules: `LeanLean.Syntax` checks 576 declaration entries with 194 additional assumptions after 148 accepted generated candidates, and `LeanLean.Kernel` checks 1626 declaration entries with 366 additional assumptions after 664 accepted generated candidates.  The rejected and unsupported generated candidates remain outside the self-check claim.

The generated-support test now pins the assumption totals and class summaries reported by `leanlean-self-check --generated-support`.  This makes a new typed-assumption class, or a changed assumption count inside an existing class, a test failure rather than an unnoticed widening of the generated-support trusted base.  A count change should accompany a written specification update and a replay test for the generated form being admitted.

The MPC boundary and adapter split are now recorded in [Minimal Principled Checker Design](design.md).  The design treats MPC as canonical kernel declaration checking: levels, expressions, contexts, conversion, and declaration admission.  `.olean` and NDJSON parsing, generated-record audits, dependency ordering, unsafe and partial policy, environment-extension extraction, and diagnostics remain adapter work over that core rather than hidden kernel rules.

The same design document now inventories the rule packages against the current implementation.  The inventory marks base dependent type theory, contexts and substitution, universe checking, declaration admission, `Prop`, the specified inductive subset, low-level quotients, core projections, natural literals, the admitted primitive reductions, and current conversion as implemented or partially implemented in LeanLean.  It also records the extraction boundary: importer, export parser, gap reports, generated-support inventory, and generated-record audits stay outside MPC.

The first standalone MPC peer-development target is recorded in the historical MPC PoC Plan.  The PoC uses four rule packages: declaration admission, natural literals, `Prop`, and simple data inductives.  It uses two adapters: a canonical script reader and a small NDJSON subset adapter.  The plan deliberately leaves quotients, projections, full primitive reductions, full inductives, `.olean` reading, unsafe or partial policy, and gap reports outside the first extraction target.

The standalone MPC peer library is in place under `MPC/`, with `MPC.Configs.Poc` selecting declaration admission, natural literals, `Prop`, and simple data inductives.  Declaration admission is an explicit manifest field rather than an implicit always-on path.  The simple inductive package admits data-only single inductives with parameters, constructor-field telescopes, strict positivity checks, generated constructors, one recursor, and iota reduction.  The PoC adapters are deliberately narrow: the script adapter reads a small native declaration format, and the NDJSON adapter translates a small export-shaped subset before auditing generated constructors and recursors outside the checker.

The next MPC stress package is recorded in the historical MPC Indexed Inductive Plan.  The package should add single indexed inductive families with dependent recursors, while leaving artifact parsing and generated-record comparison outside MPC.  The first fixture is a vector-like family over the existing `Nat` declarations, because it forces constructor-target validation, recursive hypotheses in minor premises, and iota reduction under nontrivial index terms.

The first indexed-inductive package implementation now checks the vector-like family through native MPC declaration data under `MPC.Configs.IndexedPoc`.  The generated recursor uses a canonical minor-premise shape with constructor fields first, followed by recursive hypotheses for direct recursive fields.  The first reduction test covers the recursive-hypothesis path: reducing `Vec.rec` on a `Vec.cons` target produces the cons minor applied to the fields and a recursive recursor call, then reduces that recursive call through `Vec.nil`.

The next primitive package is recorded in the historical MPC Quotient Plan.  Quotients test primitive declarations and primitive reduction rather than generated recursor synthesis.  The plan includes a minimal equality primitive because `Quot.lift` needs a typed respectfulness proof, but it leaves full equality elimination outside this package.

The quotient package now installs `Eq`, `Eq.refl`, `Quot`, `Quot.mk`, `Quot.lift`, `Quot.ind`, and `Quot.sound` through an explicit primitive declaration selected by `MPC.Configs.QuotPoc`.  Primitive type checking reports the primitive name on failure, which caught a binder-index error in the first `Quot.ind` minor-premise schema.  Normalization reduces `Quot.lift Alpha r Beta f h (Quot.mk Alpha r a)` to `f a`, and the fixture checks both inference and conversion through ordinary MPC APIs.

The next primitive package is recorded in the historical MPC Equality Plan.  Equality transport should split `Eq` and `Eq.refl` out of the quotient package, add `Eq.rec` and `Eq.ndrec`, and make quotients depend on the equality primitives being installed first.  The first fixture should check dependent transport over `Eq.refl` and constant-motive transport through ordinary inference and normalization.

The equality package first installed `Eq`, `Eq.refl`, `Eq.rec`, and `Eq.ndrec` through `MPC.Configs.EqualityPoc`.  Later mathlib probing corrected that boundary: `Eq.ndrec` is a transparent definition over `Eq.rec`, not a second primitive eliminator.  The quotient package requires equality primitives in the environment instead of installing private equality constants, and the quotient fixture replays `.equalityPrimitives` before `.quotientPrimitives`.

The next MPC package is recorded in the historical MPC Projection Plan.  Core projections should add an expression form, typing from a one-constructor structure telescope, and reduction from constructor targets.  The first fixture should use a dependent pair-like structure so the second field type depends on the first projection from the same target.

The core projection package is implemented under `MPC.Configs.ProjectionPoc`, while earlier manifests reject projection expressions.  Projection typing infers the target type, requires a one-constructor simple inductive application with the requested structure name, and instantiates the selected field type with parameters plus earlier projections from the same target.  Weak-head reduction reduces a projection whose target reduces to the constructor application, and the fixture checks a dependent-pair case where the second field type converts from `Pred (proj DPair 0 target)` to `Pred a`.

The next MPC conversion package is recorded in the historical MPC Natural Primitive Reduction Plan.  It should add the Lean 4.29 natural-number primitive table already specified for `Nat.add`, `Nat.mul`, `Nat.pow`, `Nat.sub`, `Nat.beq`, and `Nat.ble`.  The package should run before ordinary delta unfolding, and each reducer should check the reserved declaration shape before applying an overridden computation rule.

The natural primitive package is implemented under `MPC.Configs.PrimitiveNatPoc`.  Weak-head reduction now checks the six admitted natural primitive names before transparent definitions unfold, and it applies a reducer only when the manifest enables the package and the environment contains the required declaration shape.  The fixture covers numeric and boolean reductions, conversion through `Nat.add m 1`, baseline behavior without the package, malformed primitive declarations, and a malformed `Bool.true` constructor.

The next MPC conversion package is recorded in the historical MPC Function Eta Plan.  Function eta should compare a lambda with a function term by inferring the function type, checking the lambda domain, and comparing the body with the function applied under the binder.  The first tests should cover the manifest gate and an interaction with natural primitive reduction through `Nat.add m 1`.

The function eta package is implemented under `MPC.Configs.FunctionEtaPoc`.  Definitional equality now tries eta in both directions after structural conversion fails and before proof irrelevance, using the current context and universe parameters.  The fixture checks the manifest gate, both comparison directions for `fun x : Nat => f x`, and a combined manifest where eta compares `fun x : Nat => Nat.add x 1` with `Nat.succ` through the natural primitive reduction package.

The next base-theory item is recorded in the historical MPC Universe Comparison Plan.  It should replace the standalone MPC's first PoC level equality with the specified symbolic comparison used by the main checker.  The tests should cover dominated `max` summands, the valid `imax 0 u`, `imax 1 u`, and `imax u u` equations, unresolved `imax` ordering, and a negative comparison between unrelated parameters.

The MPC level comparison now uses summand normalization for `max`, partial `imax` reduction, symbolic `imax` equality, and a level-ordering predicate.  This brings the standalone base theory in line with the specified equations that broad Lean 4.29 replay needs.  The fixture checks dominated `max` summands, `imax 0 u = u`, `imax 1 u = u`, `imax u u = u`, an unresolved `imax` upper bound, and a negative comparison for unrelated level parameters.

The next MPC inductive package is recorded in the historical MPC Prop Inductive Plan.  The first slice should preserve the base PoC's data-only inductive behavior while adding a separate manifest for simple proposition-valued inductives with Prop-only recursors.  Large elimination, indexed Prop recursion, and `Acc`-shaped recursion remain separate steps.

The first Prop inductive slice is implemented under `MPC.Configs.InductivePropPoc`.  The base PoC still rejects proposition-valued inductives, while the new manifest admits simple Prop inductives only when the general Prop package is enabled.  The shared simple recursor generator now chooses a `Prop` motive and no fresh motive universe for Prop-valued inductives, and the fixture checks `PropOnly.rec` inference and iota reduction.

The same historical Prop inductive plan now includes the indexed Prop-only slice.  That step should preserve the no-large-elimination boundary while allowing indexed proof families to generate Prop-valued motives and recursors.  This is the next prerequisite for `Acc`-shaped recursion, because `Acc` is an indexed proposition-valued family with recursive proof fields.

Indexed Prop-only inductives are now implemented under `MPC.Configs.IndexedPropPoc`.  The indexed recursor generator shares the same Prop motive rule as simple Prop inductives and omits the fresh motive universe parameter for proposition-valued results.  The fixture keeps the ordinary indexed PoC rejection, then checks `PropVec.rec` inference and reduction through an indexed constructor target.

The next MPC indexed-inductive slice is recorded in the historical MPC Indexed Recursive Field Plan.  It should extend recursive-field metadata from direct recursive fields to recursive occurrences under binder telescopes.  This is the first `Acc`-shaped rule needed before artifact support can help with the gcd example.

The indexed recursive-field slice is implemented for direct fields and fields whose recursive occurrence appears under a binder telescope.  `IndexedRecursiveFieldInfo` now records the field index, local binders, and recursive target indices.  Recursor generation gives such a field a function-valued induction hypothesis, and iota reduction builds the corresponding lambda by substituting constructor parameters and fields below the local telescope.  The `Reach` fixture has the same shape as the recursive field in `Acc`: reducing its recursor on a constructor target passes the minor premise a lambda that recursively calls the same recursor on `step y h`.

The next MPC Prop-inductive slice is recorded in the historical MPC Prop Large Elimination Plan.  It should add a separate large-elimination manifest mode for conservative subsingleton Prop inductives, starting with constructors whose field types all live in `Prop`.  This keeps Prop-only recursors explicit while admitting the `Acc`-shaped elimination needed for well-founded data definitions.

The large-elimination slice is implemented under `MPC.Configs.IndexedPropLargeElimPoc`.  The eligibility check accepts proposition-valued inductives whose constructor fields are propositions or whole fields forced by constructor target indices.  This admits the `Reach` fixture's `x : Alpha` field because the constructor target is indexed by that field, while its recursive `step` field lives in `Prop`.  The large-elimination test instantiates `Reach.rec` with motive universe `Sort 1`, uses a motive returning `Nat`, and reduces the constructor case to `Nat.zero`.

`MPC.Configs.LeanCore429` now selects the standalone rule packages implemented so far: indexed inductives, Prop inductives with large elimination, equality, quotients, projections, Lean 4.29 natural primitive reductions, and function eta.  This gives artifact support a single MPC configuration to target for the gcd-oriented fixtures.  Remaining gaps should now be exposed by translating concrete artifacts rather than by adding more speculative rule packages.

The first MPC export-artifact path is `mpc-check-export`, backed by `MPC.Adapters.Export`.  It reads the real `lean4export` NDJSON tables, lowers ordinary declarations into standalone MPC declarations, treats generated constructor and recursor rows as adapter audits, and replays the result with `MPC.Configs.LeanCore429`.  The gcd script reaches MPC replay and currently rejects at `Nat.casesOn`, which makes the next task a concrete rule-package or generated-recursion diagnosis rather than a parser-design question.

Simple data inductives now record direct recursive constructor fields in their recursor metadata.  Generated minor premises bind constructor fields first, then induction hypotheses for recursive fields, and iota reduction passes recursive calls to the selected minor premise.  The native `RecNat` fixture checks the `Nat.rec` shape and a `casesOn`-style definition compiled through that recursor.  The gcd artifact now gets past the earlier `Nat.casesOn` rejection and reaches a stack overflow during full `mpc-check-export` replay, so the next artifact task is to make the diagnostic path locate the failing declaration without exhausting the host stack.

`mpc-check-export` now has diagnostic `--limit` and `--trace` options.  At this point in the work, the adapter treated exported `Eq.ndrec` as redundant equality-primitive support after the equality primitive package had installed `Eq`, `Eq.refl`, `Eq.rec`, and `Eq.ndrec`; later mathlib probing replaced that model with ordinary definition checking for `Eq.ndrec`.  With tracing enabled, the gcd artifact reaches `Nat.noConfusion`; this is generated no-confusion support in the artifact layer, not a new MPC type-theory rule.  The next gcd-oriented adapter slice should make generated no-confusion handling explicit so replay can keep probing ordinary declarations without silently accepting generated assumptions.

The diagnostic checker now exposes that boundary as `--assume-generated`.  Under that option, known generated no-confusion declarations become typed assumptions and the output reports the assumption count.  At this stage the gcd script used this option because its purpose was to find the next MPC-facing rule gap, not to claim full artifact acceptance.  With generated no-confusion assumed, tracing reaches `Nat.not_succ_le_zero`, an ordinary theorem whose proof depends on the equality and no-confusion path.

MPC conversion diagnostics now retain the original structural equality failure when eta and proof irrelevance do not close the comparison.  This exposed the next concrete arithmetic-prelude issue: `Nat.not_succ_le_zero` needs `Nat.noConfusionType` to remain transparent and needs `Nat.rec` to reduce on raw natural literals reached through the `OfNat` wrapper path.  MPC now reduces simple `Nat.rec` applications on raw natural literals, and the native fixtures cover both direct `Nat.rec` literal reduction and transparent `OfNat.ofNat Nat 0 (instOfNatNat 0)` projection reduction.  The gcd diagnostic script currently runs the traced prefix through declaration 27, just before `Eq.symm`; direct tracing shows `Eq.symm` is the next host-stack failure.

The first rejected match-helper definitions were not missing a match-helper admission rule.  Representative failures compared `OfNat.ofNat Nat 0 (instOfNatNat 0)` with `Nat.zero`; preserving the two transparent numeric-literal wrapper values in the self-check trusted base fixed those declarations without adding a primitive conversion rule.  Four match-helper definitions still reject.  Two private splitter definitions require closed `Nat.land` and `Nat.shiftRight` computation, and two `Declaration.match_on_same_ctor` helpers require a separate account of dependent same-constructor matching and no-confusion support.

The independent artifact interface is now `leanlean-check-export`.  It reads a `lean4export` NDJSON file from a path or the Arena `IN` environment variable, parses the accepted format 3.1.0 fragment, and replays the resulting declaration script without Lean's environment loader.  The generated accepted tests export `LeanLeanFaithfulness.ExportSmoke.unbox`, selected `Faithfulness.Accepted` roots, and `LeanLeanFaithfulness.ExportArithmetic.gcd_sum_diff_eq_one`, covering theorem declarations, opaque declarations, raw natural literals, quotient primitives, subtype values, compiled recursive definitions, and a small arithmetic proof over gcd and parity.  The generated rejection tests export `LeanLeanFaithfulness.Arena.Bogus1.thm` and `LeanLeanFaithfulness.Arena.ProjOfProp.badFalse`, adapted from Lean Kernel Arena source fixtures, and assert both the rejected outcome and a diagnostic fragment.  The test script uses a matching `lean4export` binary supplied by `LEANLEAN_LEAN4EXPORT`; the project does not vendor that tool.

The export checker now has a diagnostic `--gap-report` mode.  It parses the same NDJSON artifact as the accepting checker, reports ordered and dependency-aware replay outcomes, and classifies each parsed declaration as checked, generated-compared, rejected, or assumed-after-rejection.  With `--self-check-roots`, the diagnostic mode follows the rooted self-check policy and reports checked declarations, trusted-base assumptions, and trusted generated comparisons.  If parser or importer translation reaches an unsupported export entry before declaration replay, the diagnostic mode emits an unsupported row instead of returning a bare unsupported checker outcome.  Row classification follows dependency readiness in whole-artifact mode, so source-order failures do not become false assumptions when dependency-aware replay can check the declaration.  The assumed-after-rejection class is a diagnostic continuation device: after a declaration value is rejected, the report can temporarily add its type as an assumption to classify later declarations.  It does not turn the artifact into an accepted checker result, and the ordinary checker paths remain the only acceptance boundary.

Lean name translation now has an explicit injective string encoding.  The local kernel still stores names as strings, but `Lean.Name.toString` is not injective over all Lean names: a string component that itself contains Lean's quotation characters can print the same way as a distinct component that merely requires quotation.  Ordinary names keep their Lean spelling only when each string component prints without quotation and the whole spelling stays outside the reserved local prefix.  Names outside that ordinary subset use a reserved structural encoding that records each `anonymous`, `str`, and `num` constructor with length-delimited string components.  `testdata/name-encoding-collision.ndjson` preserves a collision class by checking that a single component `a.b` and the two-component name `a.b` remain distinct local constants.

The current local exporter binary is `/tmp/lean4export/.lake/build/bin/lean4export`.  With `LEANLEAN_LEAN4EXPORT` set to that path, `tools/export-pipeline-tests.sh` accepts all current generated fixtures, rejects the generated Arena-derived bad fixtures, checks the static Arena copies in `testdata/arena`, and accepts the Arena-style `$IN` case.  The gcd/parity fixture checks 572 declaration entries from an independently generated NDJSON artifact.  This confirms a larger supported NDJSON path that includes arithmetic prelude dependencies without importing Lean's module environment during checking.

The static Arena copies are `level-imax-normalization.ndjson` and `constlevels.ndjson`.  The first targets a level normalizer that wrongly equates `imax 0 v` with `succ (imax 0 v)` by dropping a successor offset.  The second targets bad constant-level unfolding, where a transparent constant is used with an invalid number of universe arguments.  These artifacts are checked as static exports because ordinary Lean source should not generate them.  The earlier `level-imax-leq.ndjson` and `nat-rec-rules.ndjson` fixtures remain candidates, but the copied versions currently reject before their intended bug on this checker because their exported `False.rec` metadata predates the local v4.29.1 regeneration behavior.

Accepting safe compiled recursive definitions exposed two kernel bugs rather than a need for a recursive-definition primitive.  First, weak-head beta reduction dropped the active universe context, so imported polymorphic helper definitions such as `Nat.brecOn.go` treated `u` as unbound while reducing.  Second, symbolic level equality kept dominated `max` summands, so it missed equalities such as `max 1 (u+1) = u+1`.  The fixes preserve the intended rule: source-recursion metadata is provenance, while the compiled core definition must still recheck through ordinary definition admission.

The gcd/parity export stress case exposed a separate class: Lean declarations whose constants have kernel-overridden computation behavior.  `Nat.le_of_lt_add_one` requires conversion to identify `m + 1` with `Nat.succ m`; Lean obtains that equality from the kernel behavior of `Nat.add`, whose prelude declaration also supplies a logical model.  Later arithmetic and character-validity dependencies required `Nat.mul`, `Nat.pow`, `Nat.beq`, and `Nat.ble` for the same reason.  Closed subtraction then entered the admitted table through the same rule: `Nat.sub` has source evidence in `Init/Prelude.lean`, a specified truncated-subtraction reduction on numeric arguments, declaration-shape checks, local implementation tests, and generated export tests for both nonnegative and truncated subtraction.  The local checker treats these constants as named primitive reduction rules before ordinary delta unfolding, and each reducer checks that the local declaration has no universe parameters, has the specified `Nat` or `Bool` result type, and is a transparent definition before it applies the rule.  Boolean-valued reductions also check that the selected `Bool.true` or `Bool.false` result is a constructor of `Bool` with type `Bool`.  These rules remain tied to the primitive table in `spec.md`; adding more arithmetic or string primitives without the same citation, rule, declaration-shape check, and tests would turn export replay into implementation folklore rather than a specified kernel.

The first source scan for similar kernel-overridden primitives found more candidates than the local checker currently admits.  `Init/Prelude.lean` documents overrides for the special representation of `Nat`, for `Nat.add`, `Nat.mul`, `Nat.pow`, `Nat.sub`, `Nat.beq`, `Nat.decEq`, and `Nat.ble`; `Init/Data/Nat/Gcd.lean` documents an override for `Nat.gcd`; and other files may contain additional primitive behavior.  `Nat.sub` has since entered the admitted table.  The remaining candidates need their own rule, declaration-shape check, implementation, and export-checker test before they enter conversion.

The source-backed primitive inventory now lives in [Lean 4.29.1 Primitive Reduction Inventory](primitive-inventory.md).  It separates admitted reductions from candidates and from runtime externs that lack kernel-reduction evidence.  The inventory keeps `Nat.div`, `Nat.modCore`, string, array, byte-array, IO, fixed-width integer, and floating-point externs outside local conversion unless later source evidence shows kernel reduction behavior.

The arithmetic export also exposed ordinary kernel-rule mismatches after the exporter path was fixed.  Quotient records and the exported `Quot.sound` axiom now coalesce into checked quotient primitive declarations; function eta participates in conversion; generated recursor reduction re-applies trailing arguments when the recursor motive returns a function; direct recursive occurrences under binders use the existing block-member motive instead of creating duplicate helper targets, as `Acc` requires; and natural literals remain raw while conversion compares their numeric value.  Level comparison records the valid `imax 1 u = u` equation and proves `imax a b ≤ c` from separate bounds on `a` and `b`, which the `Lean.Data.AC.EvalInformation` class needs.  Declaration admission checks lambda values against their declared types rather than relying only on whole-value type inference, and proof irrelevance now compares proposition types by conversion instead of pre-normalizing them.  These changes let the maintained gcd/parity fixture pass as a generated export-checker test.

The earlier `omega`-generated version of the arithmetic proof remains a separate stress artifact.  Its dependency closure reaches broad `Lean.Omega` proof-certificate machinery, and direct replay has shown slow points around `Lean.Omega.IntList.dot_mod_gcd_left` and `Lean.Omega.Constraint.tidy_sat`.  Those slow points belong to performance and resource-bound work for broad tactic-generated exports; the regular gcd fixture exercises kernel arithmetic without pulling that certificate closure.  They should become profiled performance tests before any claim that large Omega closures replay within acceptable bounds.

The standalone MPC stack overflow at `Eq.symm` came from conversion, not from equality recursor reduction.  After structural comparison failed, `defEq` tried proof irrelevance before checking that both compared terms were proofs; comparing unequal `Sort` expressions then compared their higher `Sort` types without reaching a base case.  MPC now checks that both inferred term types are propositions before it compares those proposition types, so non-proof comparisons produce finite conversion failures while proof irrelevance still closes equal proofs of the same proposition.

The same diagnosis exposed two universe-order errors in the MPC Lean 4.29 path.  Lean exports `Eq.rec` and the transparent `Eq.ndrec` abbreviation with the motive universe before the equality type universe, so the equality package uses that level-parameter order for both the primitive recursor and the checked abbreviation.  Generated recursors now choose a motive universe name fresh for the inductive's own universe parameters and place it before those parameters, which matches exported recursor applications such as `HEq.rec [0, u]` and avoids duplicate local universe names.

The next `eq_of_heq` rejection was an equality K-reduction gap, not a heterogeneous-equality special case.  The exported proof compared an `Eq.rec` transport with its minor premise when the equality endpoints were already the same expression, but the proof argument was an arbitrary local proof rather than `Eq.refl`.  The equality primitive reducer now accepts that K-style case for `Eq.rec` after weak-head reduction of the endpoints.  `Eq.ndrec` uses the same rule by unfolding to `Eq.rec`.  The native MPC fixture checks transport along a non-`refl` proof of `a = a`, and the gcd diagnostic prefix now reaches `Nat.le_trans`.  That next rejection belongs to the compiled match-helper and indexed-recursion path for `Nat.le`.

The `Nat.le_trans` rejection was a base `let` rule gap.  Lean's proof introduces `funType_1` as a local `let` value, and later conversion relies on unfolding that local value inside dependent proof types.  MPC previously checked a `let` body under a plain local assumption and substituted the value only into the inferred result type; that loses zeta conversion while checking the body.  `infer` and `check` now substitute the checked let value into the body before continuing, and the native fixture checks a nested dependent let whose inner type reduces only after zeta.  The gcd diagnostic prefix now reaches past `Nat.le_trans`.

The HAdd rejection was a projection-package substitution bug.  Projection field types instantiate the structure parameters and earlier projected fields into a source-context field type, and that operation must be simultaneous because inserted open terms may sit under binders in the field type.  MPC now has `Expr.instantiateManyFrom`, uses it for source-argument instantiation, and uses the same operation below local binders in indexed-recursive-field reduction.  The native projection fixture now includes an HAdd-shaped structure with field type `α -> β -> γ` and checks the ordinary projection accessor definition.

The `Or.casesOn` rejection refined the Prop-inductive large-elimination rule.  A simple proposition-valued inductive with more than one constructor does not get a motive universe parameter even when all constructor fields are propositions.  MPC now restricts simple Prop large elimination to zero- or one-constructor inductives whose fields pass the existing proof-or-forced-index check.  The native Prop fixture checks that a two-constructor proposition has a Prop-only recursor under the large-elimination manifest, and the gcd diagnostic prefix now reaches `Nat.modCore_lt`.

The `Nat.modCore_lt` rejection was a conversion recursion bug.  Structural comparison recursed with structural comparison on application arguments, lambda bodies, forall bodies, let bodies, and projection targets, so proof irrelevance only ran at the outer comparison.  Lean conversion needs proof irrelevance below ordinary applications because data-valued terms can take proof arguments, as `Nat.modCore.go y h ...` does.  Structural comparison now recurses through the full conversion procedure, and the native fixture checks `UseProof p = UseProof q` for two proofs of the same proposition.  The gcd diagnostic prefix now reaches `funext`.

The `funext` rejection involved two conversion details in the quotient/function-eta path.  `Quot.lift` reduction now compares the carrier and relation arguments by alpha-equivalence instead of exact syntax, because exported relation lambdas can differ only by binder names.  Function eta now inspects the weak-head form of the eta-expanded side, so transparent helpers and local definitions that reduce to lambdas can eta-compare with function variables.  The native fixtures cover both renamed quotient-relation binders and eta through a transparent definition.  The gcd diagnostic prefix now reaches through `Nat.gcd_rec`, although that theorem is a performance wall rather than a rule rejection.

The standalone MPC checker now accepts the generated gcd/parity export without generated-support assumptions: 493 replay declaration entries and environment size 571.  The three declarations that earlier entered through `--assume-generated`, `Nat.noConfusion`, `Bool.noConfusion`, and `noConfusion_of_Nat`, are exported with ordinary bodies and replay as ordinary MPC definitions or theorems.  `tools/mpc-export-gcd.sh` now runs the ordinary accepting checker path, so the maintained GCD fixture claims checked replay rather than replay modulo generated no-confusion assumptions.

`mpc-check-export` now treats replay diagnostics as structured telemetry.  `ReplayOptions` carries tracing and telemetry configuration, and each declaration replay can emit a `DeclarationTelemetry` event with index, declaration kind, name, checked-or-rejected status, elapsed milliseconds, and cumulative milliseconds.  The CLI exposes the first two renderers as `--stats` for human-readable rows and `--stats-jsonl` for downstream analysis, while the default acceptance path remains unchanged.

The first MPC performance pass is recorded in the historical MPC Performance Notes](perf.md).  Application-spine decomposition now uses an accumulator instead of repeated append, and the primitive Nat reducer admits the source-backed right-zero cases for `Nat.mul`, `Nat.pow`, and `Nat.sub`.  Partial GCD replay profiles show no meaningful timing change from either optimization, so the next performance step should instrument conversion and normalization below declaration granularity before changing fallback order or environment representation.

`mpc-check-export --profile-jsonl` now emits structural declaration counters beside elapsed replay time.  The counters record expression size, application-spine shape, constant-reference categories, and head applications for transparent definitions, recursors, equality recursors, quotient lift, and primitive Nat constants.  The first prefix-430 GCD profile shows slow declarations with many transparent-definition head applications and no primitive Nat head applications, which supports focusing the next dynamic telemetry on conversion and weak-head normalization rather than on more Nat primitive rules.

The first successful MPC performance optimization is a top-level alpha-equivalence fast path in conversion.  `defEq` now accepts terms that are already alpha-equivalent before weak-head reduction or fallback search, which cuts the full GCD export profile from 597,416 ms to 12,573 ms.  Proof irrelevance now runs before function eta after structural failure, and it checks the left inferred type for `Prop` before comparing the two inferred types instead of separately checking both sorts.  Removing detailed structural mismatch rendering and adding a post-whnf alpha check were tested but not kept.

The maintained MPC export regression path is now `tools/mpc-export-tests.sh`.  It builds the arithmetic fixture, exports `MPCFixtures.ExportArithmetic.gcd_sum_diff_eq_one`, runs `mpc-check-export` without generated-support assumptions, and checks the accepted output fragment.  This makes ordinary MPC replay of the GCD fixture a test boundary rather than a manual helper invocation.

Generated-support assumption replay now has an explicit diagnostic status.  `mpc-check-export --diagnostic-assume-generated` replaces known no-confusion support bodies with axioms before replay, but a successful run prints `diagnostic-accepted` rather than `accepted`.  The older `--assume-generated` spelling remains an alias for the same diagnostic mode, so maintained acceptance checks can reject any path that reports diagnostic acceptance.

The first broad tactic stress fixture is `MPCFixtures.ExportOmega`, with `tools/mpc-omega-stress.sh` as the profiling entry point.  A small `omega` proof exports a large dependency closure; the first run exposed disabled raw string literals at `String.instInhabited`, so LeanCore429 now enables neutral string literals while the smaller PoC manifest keeps natural literals only.  The next run reaches `Lean.Omega.normalize_sat` and later rejects the inductive `Lean.Syntax`, whose `args` field requires a stricter account of nested positive occurrences through specified container types.

The next rule slice is recorded in the historical MPC Nested Positivity Plan.  It keeps the base PoC direct-only, adds a LeanCore429 manifest mode for specified covariant containers, and starts with `Array` and `List` from Lean 4.29.1.  The first implementation should move the `Lean.Syntax` rejection only by admitting `Array Syntax` as a positive constructor field; nested induction hypotheses remain a separate recursor-generation question if later declarations require them.

LeanCore429 now enables nested positivity through the specified unary containers `Array` and `List`, while the base PoC manifest remains direct-only.  The rule requires the container constant to exist as an admitted inductive before it can justify a recursive occurrence, and it checks the covariant argument structurally, so `Array Syntax` is accepted but `Array (Syntax -> Nat)` is still rejected.  The Omega stress artifact now checks the exported proof and then rejects during generated-recorder audit because Lean's nested-inductive machinery expects `Lean.Syntax.rec_2`, which is a separate nested-recursor boundary.

The nested recursor boundary is recorded in the historical MPC Nested Recursors Plan.  The export record for `Lean.Syntax` contains three generated recursor targets: the root `Lean.Syntax.rec`, plus helper targets `Lean.Syntax.rec_1` and `Lean.Syntax.rec_2` for the nested `Array Syntax` and `List Syntax` shapes.  The next implementation should port the family-target model into MPC rather than adding those names as unchecked audit exceptions.

MPC now generates a first nested recursor family for one-root simple inductives whose recursive fields pass through the specified unary Lean 4.29 containers.  The implementation discovers helper targets from the admitted inductive declarations, instantiates container constructors such as `Array.mk` and `List.cons`, generates the root and helper recursor types, and stores those constants under a distinct `.nestedRecursor` kind.  The export adapters accept generated recursor rows only when those names exist in the MPC environment as generated recursors, so `Lean.Syntax.rec_1` and `Lean.Syntax.rec_2` no longer enter through an audit exception.  Normalization now reduces saturated nested recursor applications by selecting the target-specific minor premise and recursively applying the appropriate root or helper recursor to recursive fields.  The Omega stress artifact now accepts through ordinary replay, checking 1245 declaration entries with environment size 1409, which shows that the immediate `Lean.Syntax.rec_2` boundary was missing generated-family admission rather than a need for generated-assumption policy.

Nested recursive fields now support local function telescopes.  A field such as `(n : Nat) -> List T` contributes a function-valued induction hypothesis whose body calls the generated helper recursor on the field applied to the local arguments.  The type generator lifts the local binder types across the motive and minor-premise prefix, and reduction builds the corresponding lambda before applying the constructor minor premise.  The native `NestedFn` fixture checks admission of this shape and reduces a root recursor whose minor premise invokes the generated function-valued induction hypothesis.

The next nested-recursion gap now has two native expected-rejection tests.  `NestedIndexedClosed` uses a recursive field `Vec T 0`, showing that indexed helper containers still need a specified covariant-container rule.  `NestedIndexedLocal` uses a field `(fuel : Nat) -> Vec T fuel`, showing the schema target needed for measure-indexed recursion: the helper target depends on a local binder, so a closed target expression cannot represent the family.  These tests keep MPC unchanged and pin the missing rule before the helper-target schema refactor.

Nested recursor targets now have an explicit local telescope field.  Existing helper targets still use an empty telescope, so this checkpoint preserves the accepted `List T` and `Array T` behavior while giving the indexed-container rule a representation for targets such as `Vec T fuel`.  The recursor type generator now forms motive and target binders through that schema representation, but the indexed-container rule remains disabled until the covariance and constructor-target rules are specified.

Indexed nested helper targets now work for the specified covariant `Vec` shape.  Recursive fields store the helper target arguments that instantiate the target-local telescope, and helper constructors store the result-index arguments needed to choose an iota branch.  The closed-index fixture reduces through `NestedIndexedClosed.rec_1` on `Vec.nil`, while the local-index fixture admits `(fuel : Nat) -> Vec T fuel` and records the one-binder helper target schema.

Parameterized indexed helper targets exposed a separate context distinction.  Container parameters must be lowered out of constructor-field and recursive-field-local contexts before they define the helper schema, while constructor result indices and recursive target arguments keep the original field context because they may mention constructor fields.  The parameterized fixtures now cover both `Vec (T A) 0` reduction and `(fuel : Nat) -> Vec (T A) fuel` replay, which pins the schema-parameter rule separately from target-index substitution.

The exported nested-indexed fixture adds the same shape through Lean source and `lean4export`.  It showed that a fixed container-name list was too narrow: Lean exports the user-defined indexed container `IVec` with ordinary inductive metadata, and the nested inductive records `numNested = 1` rather than relying on a prelude name.  MPC now recognizes a conservative single-parameter covariant container by inspecting the admitted inductive declaration, and the export regression checks both `NestedIndexedParamClosed Nat` and `(fuel : Nat) -> IVec (NestedIndexedParamLocal α) fuel` through generated recursor audit.

Container covariance is now recorded as a per-argument flag vector rather than a single positive argument.  MPC computes the vector as a greatest fixed point over the admitted inductive declaration, marks indexed-family indices non-positive, and uses the same flags in positivity checking and nested helper-target discovery.  Native and exported fixtures now cover `PairBox α T`, `IBox α T n`, and rejection of a two-parameter container whose recursive argument occurs in a function domain.

The mutual-inductive block slice is recorded in the historical MPC Mutual Inductive Plan.  MPC now has an explicit `inductiveBlocks` manifest switch and a block declaration that admits simple mutual data types through a provisional environment containing every block member.  The first implementation generates one recursor per member, one motive per member, and one minor premise per constructor across the block; native and exported even/odd fixtures check cross-member recursion, a source definition that applies `MEven.rec`, reduction through the corresponding member recursor, generated-recorder audit, and rejection of a negative cross-member occurrence.

The inductive rule-package code now lives under `MPC/Packages/Inductive` instead of in the checker spine.  `Basic.lean` contains shared telescope, target, and constructor-type helpers; `Positivity.lean` contains strict positivity and covariant-container inference; `Recursor.lean` contains simple, nested, indexed, and mutual recursor construction; `Admission.lean` contains environment admission for simple, indexed, and mutual inductives; `Reduction.lean` contains inductive recursor iota reduction behind a `whnf` callback to avoid an import cycle with normalization; and `Prop.lean` retains the Prop-elimination side conditions and recursor universe selection.  `Replay.lean` is now declaration orchestration, `Normalize.lean` handles normalization dispatch plus non-inductive reductions, and `Check.lean` no longer owns inductive package internals.

The top-level MPC package pass moved the literal, projection, and Lean 4.29 Nat primitive helpers out of the checker and normalizer spines.  `MPC/Packages/Literal.lean` owns raw literal environment requirements and the Nat-constructor view used by conversion, `MPC/Packages/Projection.lean` owns projection typing and constructor-target projection reduction, and `MPC/Packages/PrimitiveNat.lean` owns the source-backed Nat primitive reduction table behind a `whnf` callback.  `Check.lean` and `Normalize.lean` still dispatch the recursive typing and conversion procedures, while the remaining empty wrappers no longer import `Replay`; `Declaration`, `Prop`, and `FunctionEta` need a cleaner callback or orchestration boundary before moving code into them.

The new MPC export-backed self-check baseline is `tools/mpc-export-self-check.sh`.  It uses `mpc-export-roots --self-check` to choose source-facing roots, exports `MPC.Name`, `MPC.Basic`, `MPC.Level`, `MPC.Expr`, `MPC.Context`, and `MPC.Manifest` through `lean4export`, and checks each artifact with `mpc-check-export` through whole-artifact replay.  The first `MPC.Level` probe exposed a projection typing bug: polymorphic structure field types were not instantiating the structure universe parameters, so `MPC.Level.reduceIMax._proof_1` left `u` open while checking the generated well-foundedness proof.  `MPC.Packages.Projection.fieldType` now instantiates those levels before source arguments, and the native projection fixture checks the same shape.  The `MPC.Level` artifact now accepts after the environment-indexing performance fix; the remaining cost is proof-heavy conversion rather than a rule rejection.  `MPC.Expr` now accepts as the first syntax-layer self-check target, covering expression constructors, lifting, substitution, level instantiation, application-spine decomposition, and alpha equality.  `MPC.Context` extends that baseline through binder records, context lookup, context extension, and forall binding.  `MPC.Manifest` adds rule-package configuration enums, validation, and support predicates.

Targeted declaration profiling now exists for `mpc-check-export`.  `--profile-declaration <n>` replays the prefix before declaration `n` and emits the same structural declaration counters without checking the selected declaration, which handles cases where the ordinary `--profile-jsonl` row would appear only after a long wait.  The current `MPC.Level` prefix-1175 run accepts, but `Lean.Omega.IntList.dot_mod_gcd_left` takes 81,846 ms and its static profile points to transparent-definition conversion rather than primitive Nat reduction, projection reduction, or nested-recursion reduction.

MPC environments now keep a name index beside the declaration list.  The chronological-list experiment showed that linear lookup order dominated the `MPC.Level` wall: moving old constants to the front cut the slow theorem from about 82 seconds to about 21 seconds, which identified environment search rather than a kernel rule gap.  The indexed representation cuts the same theorem to about 16 seconds and lets the full `MPC.Level` export artifact accept in 34,288 ms.

The first `MPC.Env` export self-check probe rejected before it reached a useful environment-API boundary.  The `Std.HashMap` dependency closure reaches `Nat.Linear.Poly.denote_reverse`, where replay currently rejects a conversion between a `PProd` projection and a `Prod.rec` form inside a library proof.  That failure should not be counted as evidence about `Env.find?`, `Env.add`, or `ConstantInfo` instantiation.  The next design decision is whether the MPC environment index remains an explicit dependency on Lean's `Std.HashMap` implementation, or whether the checker should use a local minimal index whose proof and runtime dependency closure stay inside the MPC self-check target.

The `Nat.Linear.Poly.denote_reverse` rejection was a missing structure-recursion eta rule.  Lean's WHNF implementation rebuilds a stuck structure major premise as the constructor applied to projections before it applies the ordinary recursor rule; the local rule now implements the same behavior for non-recursive, data-valued, one-constructor simple inductives when the projection package is enabled.  The native projection fixture uses a stuck dependent `DPair` value and checks both first-field and dependent second-field recursor reduction, while the standalone export for `Nat.Linear.Poly.denote_reverse` now accepts.  Retrying the `MPC.Env` export artifact moved past the old rejection and reached `Array.toList_map`, where replay needs the companion constructor-form structure eta rule for `Array.mk α (Array.toList α xs) = xs`; that run also exposed proof-conversion cost, including 115,973 ms for `_private.Init.Data.BitVec.Lemmas.0.BitVec.toNat_sub_of_le._proof_1_2`, 219,939 ms for `_private.Init.Data.Nat.Bitwise.Lemmas.0.Nat.le_of_testBit._proof_1_2`, and 108,466 ms for `_private.Std.Data.DHashMap.Internal.Defs.0.Std.DHashMap.Internal.Raw₀.expand.go._unary._proof_1`.

Constructor-form structure eta now runs as a conversion fallback before proof irrelevance and function eta.  The rule applies only when one side is a saturated constructor of a non-recursive, data-valued, one-constructor simple inductive, the other side is not constructor-headed, both sides have definitionally equal inferred types, and every constructor field is definitionally equal to the corresponding core projection from the other side.  The native dependent `DPair` fixture checks both constructor orientations, and the standalone export for `Array.toList_map` now accepts.

`MPC.Env` now accepts through whole-artifact export replay.  Replaying `.lake/build/mpc-export-self-check/mpc-env.ndjson` checked 2,213 declaration entries and produced an MPC environment of size 2,583.  The self-check script now includes `MPC.Env`, which promotes the environment layer into the maintained self-check baseline while keeping the cost visible: the replay took about ten minutes on the current machine.

`MPC.Declaration` now accepts as the next self-check layer above `MPC.Env`.  Replaying `.lake/build/mpc-export-self-check/mpc-declaration.ndjson` checked 256 declaration entries and produced an MPC environment of size 384.  The maintained self-check script includes it after `MPC.Env`, so the declaration syntax layer is now covered before package-specific replay code.

A checked-environment layer looks promising as the middle ground between prefix caching and per-declaration dependency caching.  The interrupted `MPC.Packages.Literal` replay used an artifact with 164,231 NDJSON rows, close to the 168,067 rows in the accepted `MPC.Env` artifact, even though the literal package has only three self-check roots.  Comparing the two artifacts by resolved declaration content shows 2,067 shared primary declarations, all with identical resolved hashes; the only primary declarations absent from the `MPC.Env` layer are `MPC.Packages.Literal.requireStringSupport`, `MPC.Packages.Literal.requireNatSupport`, `MPC.Packages.Literal.natConstructorSpine.match_1`, and `MPC.Packages.Literal.natConstructorSpine`.  Raw exported declaration rows undercount reuse slightly because the artifacts share their first 155,420 NDJSON rows byte-for-byte and then use different table IDs for later declarations, but resolving names, levels, and expressions removes those incidental differences.  This supports a layer design that stores checked environment entries plus resolved declaration hashes, lets a replay skip matching names already present in the layer, and checks only declarations whose resolved hash is absent.

The first checked-layer implementation is in-memory and non-persistent.  `mpc-check-export --checked-layer <base.ndjson> <target.ndjson>` cold-checks the base artifact, records a content key for each lowered MPC declaration and every constant added by that declaration, then replays the target artifact from the checked base environment.  If a target declaration has the same lowered content key as a layer declaration, the replay reuses it; if an anchor name exists in the layer with different content, the replay rejects; otherwise it calls the ordinary checker.  A self-layer replay of `mpc-declaration.ndjson` reused all 256 lowered declaration entries and checked zero.  Replaying `mpc-packages-literal.ndjson` against `mpc-env.ndjson` reused 2,062 lowered declaration entries, checked four literal-package entries, and produced environment size 2,587.  The mode reports `layer-accepted`, not `accepted`, because the target run uses a prechecked environment.  Persistent layer files are the next step if this should reduce wall-clock time rather than prove the reuse rule.

Checked layers can now be saved and loaded with `mpc-check-export --save-layer <layer.json> <export.ndjson>` and `mpc-check-export --load-layer <layer.json> <target.ndjson>`.  The layer file stores a format version, manifest name, checked `Env.entries`, and the lowered declaration-content table; loading rebuilds the environment index from the entries rather than serializing `Std.HashMap`.  Saving `.tmp/mpc-env.layer.json` from the `MPC.Env` artifact cold-checked 2,213 declaration entries and produced a 417,651,533-byte JSON file.  Loading that file against `mpc-packages-literal.ndjson` reused 2,062 declaration entries, checked four entries, produced environment size 2,587, and completed in about eight seconds on the current machine.

Checked-layer persistence now has a SQLite PoC behind the same `--save-layer` and `--load-layer` options, selected by `.sqlite`, `.sqlite3`, or `.db` suffixes.  The checker boundary is unchanged: SQLite storage lives in `MPC.Adapters.Layer`, stores the same checked environment entries and declaration-content records as JSON rows, and reconstructs an ordinary `CheckedLayer` before replay.  The implementation uses the local `sqlite3` executable through Lean's `IO.Process` API rather than adding a Lake dependency or FFI package; the `MPC.Declaration` layer round trip saved 384 environment rows and 256 content rows, then reused all 256 declaration entries on load.

The SQLite layer format now uses SQLite as an online store rather than a wrapper around a fully materialized `CheckedLayer`.  Saving a `.db` layer checks declarations and writes accepted environment rows and content-key rows during replay, committing only after the generated-declaration audit succeeds.  Loading a `.db` layer streams environment rows into an ordinary MPC `Env`, then asks SQLite for content matches for the target declarations instead of reading the whole content table into a `Std.HashMap`.  Format version 2 stores content as `content(key TEXT PRIMARY KEY, names TEXT NOT NULL) WITHOUT ROWID`, which avoids duplicating the large content key in a separate primary-key index.  The rowidless `MPC.Env` DB is `.tmp/mpc-env.layer-v2-without-rowid.db`: it is 398M, contains 2,583 environment rows and 2,213 content rows, reuses all 2,213 declarations in the `MPC.Env` self-load case in 14.522 seconds, and reuses 2,062 declarations while checking four `MPC.Packages.Literal` declarations in 14.423 seconds.

`tools/mpc-export-self-check.sh` now uses the v2 SQLite layer for targets at or above `MPC.Env` when `.tmp/mpc-env.layer-v2-without-rowid.db` exists, with `LEANLEAN_MPC_LAYER_DB` as the override.  Smaller bootstrap artifacts still run cold because the `MPC.Env` layer can contain declarations with the same names but different exported content under smaller root sets; `MPC.Basic` exposed that boundary at `Except`.  The maintained script now includes `MPC.Packages.Literal`: the cached run reuses 2,062 declarations, checks `MPC.Packages.Literal.requireStringSupport`, `MPC.Packages.Literal.requireNatSupport`, `MPC.Packages.Literal.natConstructorSpine.match_1`, and `MPC.Packages.Literal.natConstructorSpine`, and produces environment size 2,587.

The next package probes exposed a cache equality issue before they exposed a rule-package issue.  `MPC.Packages.Equality` and `MPC.Packages.PrimitiveNat` both reached `instDecidableOr._proof_1` with the same exported Lean name and alpha-equivalent theorem body, but the old layer content key used `repr declaration`, so a binder-name difference made the declaration miss the content table and then trip the same-name conflict check.  Layer replay now treats an existing atomic declaration as reusable when the cached constant has the same name, kind, universe parameters, and alpha-equivalent type and value.  This keeps the checker unchanged and preserves rejection for same-name declarations whose bodies differ beyond alpha-renaming.  A native layer test covers the case with a theorem whose binder names differ between the saved layer and target replay.  The cached primitive-Nat probe now reaches the package tail; bounded runs through declaration 2,000 reuse 1,985 declarations and check 15, while the later tail spends its time on generated `ConstantKind` and `Std.HashMap` helper declarations rather than on a primitive-Nat rule rejection.

SQLite layer replay now supports `--stats` and `--stats-jsonl`.  The event stream marks each target declaration as reused, checked, or rejected and records elapsed and cumulative milliseconds, while `--profile-jsonl` remains disabled for layer replay because the current profile implementation belongs to cold source replay.  Running the primitive-Nat package tail with `--stats-jsonl --limit 2150 --load-layer .tmp/mpc-env.layer-v2-without-rowid.db` showed progress through index 2148, `MPC.ConstantInfo.casesOn`, after 6,716 ms; the next declaration is `MPC.Packages.PrimitiveNat.boolCtorExpr._sparseCasesOn_1`, the 75k-character generated matcher over `ConstantKind`.  That gives the next performance task a concrete target without changing any rule package.

`MPC.Packages.PrimitiveNat.boolCtorExpr` now disables Lean's sparse match generation for its local `ConstantKind` match with `set_option backward.match.sparseCases false`.  Lean's source describes sparse cases as an auxiliary matcher with a `Nat.hasNotBit` catch-all proof; that representation was the slow declaration in this package, while the ordinary full match checks quickly under the existing rule packages.  This is a source-generation choice for the MPC package, not a general solution for arbitrary Lean exports that contain sparse cases.  The maintained export self-check script now includes `MPC.Packages.PrimitiveNat`; with the v2 `MPC.Env` SQLite layer, it reuses 2,100 declarations, checks 66 package-tail declarations, and produces environment size 2,690.

`MPC.Packages.Equality` now uses a package-local comparison for equality primitive `ConstantKind` constructors instead of derived `BEq` over the full `ConstantKind` type.  The derived comparison pulled in BEq declarations for unrelated inductive metadata and then spent time in generated same-constructor helpers and expression BEq helpers.  The narrowed comparison also disables sparse match generation for the same reason as the primitive-Nat Bool constructor check.  The maintained export self-check script now includes `MPC.Packages.Equality`; with the v2 `MPC.Env` SQLite layer, it reuses 2,106 declarations, checks 71 equality-package declarations, and produces environment size 2,695.

`MPC.Packages.Quotient` now accepts as a cached export self-check target without a source change.  The package benefits from the equality package cleanup because its closure includes the equality primitive helpers.  With the v2 `MPC.Env` SQLite layer, the quotient target reuses 2,106 declarations, checks 93 quotient-package declarations, and produces environment size 2,717.

`MPC.Packages.Projection` now disables sparse match generation around the structure-shape and constructor-kind matches in `fieldType` and `reduce?`.  The first cached projection probe otherwise stalled at generated sparse matchers over `ConstantInfo` and `ConstantKind`, not at a projection rule rejection.  With ordinary match generation, the projection package target reuses 2,100 declarations, checks 151 projection-package declarations, and produces environment size 2,806.

`MPC.Packages.Inductive.Basic` now accepts as the first inductive-package export self-check target.  This module contains shared telescope, target, constructor-type, and motive-type helpers rather than inductive admission rules.  With the v2 `MPC.Env` SQLite layer, it reuses 365 declarations, checks 40 package-tail declarations, and produces environment size 2,623.

`MPC.Packages.Inductive.Positivity` now accepts as the first inductive rule self-check target.  The first probe stalled in the generated sparse matcher for `availableFixedCovariantContainer?`, which checks the fixed Lean 4.29 nested-container metadata rather than the strict-positivity rule.  The source now disables sparse match generation for that local metadata function; with the v2 `MPC.Env` SQLite layer, the positivity target reuses 2,055 declarations, checks 65 package-tail declarations, and produces environment size 2,691.

`MPC.Packages.Inductive.Recursor` now accepts as the first recursor-construction export self-check target.  The slow points were generated sparse matchers for `nestedContainerOccurrence?` and `specForNestedTarget`, both of which classify nested helper targets rather than applying recursor rules.  The source now disables sparse match generation for those local classifiers; with the v2 `MPC.Env` SQLite layer, the recursor target reuses 2,137 declarations, checks 314 package-tail declarations, and produces environment size 2,989.

`MPC.Packages.Inductive.Admission` now accepts as the inductive environment-admission export self-check target.  It adds the checked admission paths for simple, indexed, and mutual inductives on top of the already checked recursor helpers.  With the v2 `MPC.Env` SQLite layer, the admission target reuses 2,141 declarations, checks 231 package-tail declarations, and produces environment size 2,868.

`MPC.Packages.Inductive.Reduction` now accepts as the inductive recursor-reduction export self-check target.  The first cached probe exposed an adapter limitation: the layer rejected an alpha-equivalent exported `Functor` structure because inductive declarations had no existing-declaration alpha fallback.  The layer now reuses alpha-equivalent simple, indexed, and block inductive declarations; after disabling sparse match generation for the local recursor-reduction dispatch functions, the reduction target reuses 2,147 declarations, checks 129 package-tail declarations, and produces environment size 2,753.

`MPC.Packages.Inductive.Prop` now accepts as the Prop-specific inductive export self-check target.  It covers Prop-inductive enablement, large-elimination eligibility, fresh motive-level selection, and recursor-level parameter selection.  With the v2 `MPC.Env` SQLite layer, the Prop target reuses 2,062 declarations, checks 59 package-tail declarations, and produces environment size 2,683.

`mpc-check-export --cache-layer <layer.db>` now provides a generic mutable checked-declaration cache.  If the SQLite DB is missing, the checker creates an empty layer with the current manifest and format metadata; if it exists, the checker loads the checked environment and content table, reuses matching declarations, checks misses, runs the generated-declaration audit, and appends newly checked environment rows plus new content keys in one transaction.  The old `--load-layer` mode remains read-only, and `--save-layer` still creates a fresh layer.  `tools/mpc-export-self-check.sh` now uses `--cache-layer` whenever `MPC_CACHE_DB` names a DB, defaulting to `.tmp/mpc-self-check-cache.db`.  A fresh-cache self-check run grew `.tmp/mpc-self-check-cache-20260505.db` to environment size 3,427; a second run against that DB reused every target declaration and checked zero declarations.

`MPC.Normalize` now accepts as the first checker-spine export self-check target above the rule packages.  The target checks `whnf`, `normalize`, `reduceEqRec?`, and `reduceQuotLift?`, so it exercises normalization dispatch across inductive recursor reduction, projections, primitive Nat reduction, equality transport, and quotient lift without moving into inference or replay orchestration.  With the generic SQLite cache populated by the package self-checks, the first normalize replay reused 2,060 declarations, checked four declarations, and produced environment size 3,431.

`MPC.Check` now accepts as the next checker-spine export self-check target.  The target checks inference, checking, sort inference, structural conversion, proof irrelevance, function eta, structure eta, level closure, and proposition classification.  With the same generic SQLite cache, the first check replay reused 2,217 declarations, checked thirteen declarations, and produced environment size 3,444.

`MPC.Replay` and `MPC.Configs.LeanCore429` now accept as the remaining non-adapter spine self-check targets with source-facing roots.  `MPC.Replay` checks declaration orchestration through `addDecl` and `replay`; with the generic SQLite cache, its first replay reused 2,449 declarations, checked four declarations, and produced environment size 3,448.  `MPC.Configs.LeanCore429` checks the assembled Lean 4.29 manifest; its first replay reused fifteen declarations, checked two declarations, and produced environment size 3,450.  `MPC.DefEq` and the top-level package-wrapper modules have no source-facing roots, so they do not add maintained self-check targets.

The maintained MPC export self-check now includes every non-adapter MPC module with source-facing roots.  The final inventory added `MPC.Error`, which checks `Error.withPrefix`, and the small PoC configuration modules that assemble manifest variants for equality, quotients, projections, primitive Nat reduction, function eta, simple Prop inductives, indexed inductives, indexed Prop inductives, and indexed Prop large elimination.  With the generic SQLite cache, `MPC.Error` reused 322 declarations and checked one declaration; each new derived configuration checked one manifest definition, while `MPC.Configs.Poc` and `MPC.Configs.LeanCore429` were already present in the cache.

`spec.md` has been rewritten as an MPC-only specification.  The new document removes the old LeanLean module-checker and self-check interface material, treats adapters, generated-record audits, telemetry, and SQLite caching as external boundaries, and specifies the standalone rule packages selected by MPC manifests.  It also records conformance obligations for inductive universe bounds, sort-polymorphic inductive restrictions, Prop projection restrictions, and classified resource failures, so later code work has an explicit target rather than relying on historical checker behavior.

The repository now keeps only MPC code and the files needed to drive it.  The old LeanLean executables, faithfulness harness, Arena smoke scripts, static export fixtures without a remaining driver, and stale paper or traceability documents were removed.  The remaining generated-export fixtures moved to `MPCFixtures`, the package is named `MPC`, and the surviving scripts under `tools/` use `MPC_LEAN4EXPORT`, `MPC_EXPORT_TEST_DIR`, `MPC_PROFILE_DIR`, `MPC_STRESS_TIMEOUT`, and `MPC_CACHE_DB`.  The export self-check uses a SQLite cache by default, and `MPC_CACHE_DB=` is the explicit cold-run override.

`README.md` now describes MPC from the current repository state rather than the old project lineage.  It adds goals, design, status, and running sections, with the design table matching the present code layout and adapter boundary.  The status section records the maintained verification surfaces and the stale-cache caveat, so a new reader can distinguish checker functionality from driver and cache behavior.

The root plan files have been removed.  They recorded completed package slices and duplicated the current state now captured by `spec.md`, `design.md`, `README.md`, `perf.md`, and this journal.  Open work should be stated in those current documents rather than kept as separate root-level scaffolding files.

`perf.md` now starts with the current measurement procedure.  It distinguishes cold replay from SQLite checked-layer replay, lists the maintained performance commands, explains when to use `--stats-jsonl`, `--profile-jsonl`, and `--profile-declaration`, and records the cache-mode restrictions that affect comparable measurements.  Future performance notes should include the exact artifact, command, cache mode, declaration count, environment size, and elapsed replay time.

`design.md` has been rewritten as the MPC architecture note.  It now records the checker boundary, file organization, package ownership, adapter rules, checked-layer behavior, and maintained drivers.  The document remains separate from `spec.md`: the specification states the typing and conversion rules, while the design note states where those rules live and which repository code is allowed to translate, cache, profile, or audit around them.

`metatheory.md` has been rewritten for the MPC-only repository.  It removes the old Lean importer and faithfulness-harness theorem statements, then restates the proof obligations around canonical declaration scripts, manifests, rule-package preservation, adapter theorems, checked-layer replay refinement, and resource classification.  The evidence table now points to current MPC tests, generated export scripts, self-check artifacts, name encoding, and profiling drivers.

`helper-target-design.md` has been removed from the root.  Its implemented design now belongs to the current specification, architecture note, metatheory note, and historical journal entries rather than a stale standalone design file.  `.gitignore` now ignores `.tmp`, matching the cache and profiling paths used by the maintained scripts.

`mathlib-plan.md` records the first plan for using mathlib as an external MPC probe corpus.  The plan keeps mathlib outside this repository's Lake dependencies, starts with targeted exported roots rather than bulk checking, and treats each run as gap discovery over the existing `LeanCore429` checker configuration.  It also defines result classes so diagnostic continuations, performance failures, unsupported artifact forms, audit mismatches, and accepted artifacts remain separate outcomes.

The first mathlib probe path now works.  A temporary external mathlib checkout at tag `v4.29.0`, run under the local Lean 4.29.1 toolchain, built `Mathlib.Data.Nat.Basic` and a scratch module `Mathlib.MPCProbe.Scratch`.  Exporting `MPCProbe.MathlibScratch.addZeroProbe` with the local Lean 4.29.1 `lean4export` binary produced a 639-row, 36 KB artifact; cold `mpc-check-export --stats-jsonl` accepted it with 23 checked declaration entries and environment size 39.  `tools/mpc-mathlib-probe.sh` captures the reusable external-driver path without adding mathlib to this repository's Lake dependencies.

The first real mathlib roots give a useful boundary.  `Nat.succ_injective`, `Nat.instNontrivial`, `Nat.instLinearOrder`, and `Nat.set_induction` accept through the external probe path, with `Nat.instLinearOrder` checking 332 declaration entries and producing environment size 457.  `Quot.congr_mk` from `Mathlib.Logic.Equiv.Defs` first rejected at declaration index 337 after checking `Quot.congr`, because conversion left a `Quot.lift` application stuck against `Quot.mk`; after the WHNF spine fix, the same root accepts with 338 checked declaration entries and environment size 459.  `SetLike.ext`, the selected `Quotient` roots, the selected `Fin` roots, several `Finset` roots, and the first selected algebra roots then accepted; `Finset.range_filter_eq` is the first current performance boundary, concentrated in an Omega-generated private proof from Lean's `Init.Data.List.Nat.Range`.

The polynomial mathlib probe exposed a real equality-boundary error.  `Polynomial.C_mul` reached `_private.Mathlib.Data.Finsupp.Single.0.Finsupp.update._proof_2`, where conversion compared `Eq.rec` with `Eq.ndrec` at the same universe levels.  Lean's source defines `Eq.ndrec` as a transparent abbreviation over `Eq.rec`, so MPC now keeps only `Eq`, `Eq.refl`, and `Eq.rec` in the equality primitive core and checks exported `Eq.ndrec` as an ordinary definition.  The SQLite cache loader filters the old cached `Eq.ndrec` primitive entry and tracks stored row count separately, so existing DB caches can keep their accepted declarations while appending the new checked definition at the correct table position.  After the fix, `Polynomial.C_mul` accepts with the shared mathlib cache, reusing 3,942 declaration entries, checking 348 new entries, and producing environment size 5,777.

The first linear-algebra probe found the next equality-rec boundary before reaching the requested `LinearMap.vecCons_apply` root.  Replay rejected `Fin.cons_zero` because an `Eq.rec` cast compared a tuple head `x` with a transported version of `x`, and the cast endpoints were two definitionally equal `Fin` values whose proof fields were not alpha-equivalent.  The conversion path now has a defEq-side equality-rec reduction: after ordinary structural conversion fails, it reduces an `Eq.rec` redex to its minor premise when the endpoints are definitionally equal under the active context.  This keeps weak-head reduction cheap while allowing the K-style case to use proof irrelevance and structure conversion where Lean's kernel does.  The native fixture uses a small constructor with a Prop field to check the proof-field case, and `LinearMap.vecCons_apply` now accepts with the shared mathlib cache, reusing 742 declarations and checking 25 new entries.

`Real.continuous_sqrt` repeats the rational proof boundary rather than exposing a real-analysis-specific issue.  Cache stats replay reached `Rat.addCommGroup._proof_1` at declaration index 3290 after reusing the prefix through `Rat.one_mul` in 4,673 ms.  The run timed out at the same 95-type-node, 2,575-value-node theorem already isolated from `Mathlib.Data.Rat.Lemmas`.

`Continuous.matrix_mul` from `Mathlib.Topology.Instances.Matrix` accepts through the shared mathlib cache.  The external Lake build is the main cost because the module imports determinant, multilinear, polynomial, group-action, and topology-algebra dependencies before export.  MPC replay reused 2,040 declaration entries, checked 65 new entries, and produced environment size 6,577, so this probe broadened the imported mathlib surface without exposing a new MPC rule boundary.

`Continuous.matrix_det` from the same module also accepts, with a larger target slice.  The first stats run reused 5,546 declaration entries, checked 1,495 new entries, produced environment size 9,820, and measured 240,657 ms of replay time.  A cache-only rerun then reused all 7,041 target declarations, confirming that the long first replay populated the shared SQLite cache rather than leaving a hidden semantic boundary.

`LinearMap.det_comp` reaches a generated-support performance boundary before the determinant theorem.  Cache stats replay reused the prefix through declaration index 7357 in 13,926 ms, then timed out at index 7358, `LinearEquiv.noConfusion`.  The generated definition has 699 type nodes and 8,297 value nodes, with applied heads dominated by `Semiring.toNonAssocSemiring`, `HEq`, `LinearEquiv`, `RingHomInvPair`, `RingHom`, and `Module`, so this probe belongs to the derived-support discussion rather than a new MPC rule package.

The category-theory probe first exposed a harness error.  The root `Functor.map_comp_assoc` is not present outside the `CategoryTheory` namespace, and `lean4export` emitted a panic plus a metadata-only NDJSON file.  The mathlib probe driver now captures exporter diagnostics, removes stale artifacts before export, and rejects metadata-only output before invoking MPC, so an unknown root cannot become a false zero-declaration acceptance.

The corrected category-theory root `CategoryTheory.Functor.map_comp_assoc` accepts through the shared mathlib cache.  The probe covers a category-theory structure declaration and a `grind` proof over composition associativity, with universe-polymorphic category instances in the exported closure.  MPC reused 340 declaration entries, checked 21 new entries, and produced environment size 6,606.

`CategoryTheory.isIso_of_fully_faithful` accepts as the next category-theory probe.  The target exercises fully faithful functor data, isomorphism reflection, and the ordinary instance declarations around `IsIso`, while reusing most of the category cache from the earlier functor probe.  MPC reused 322 declaration entries, checked 16 new entries, and produced environment size 9,842.

`CategoryTheory.Adjunction.comp_unit_app` accepts as an adjunction-composition probe.  The target checks the unit component for a composed adjunction, including natural transformation, whiskering, unitor, and associator declarations.  MPC reused 455 declaration entries, checked 83 new entries, and produced environment size 11,237.

`CategoryTheory.Limits.pullback.map_comp` accepts as the first finite-limits category probe.  Building `Mathlib.CategoryTheory.Limits.Shapes.Pullback.HasPullback` added comma categories, Yoneda, adjunctions, cone infrastructure, and pullback constructions to the external checkout.  MPC reused 373 declaration entries, checked 114 new entries, and produced environment size 10,159.

`CategoryTheory.Limits.pushout.map_comp` accepts as the colimit-side companion in the same module.  The run reused the pullback build and most of the category cache, then checked 73 new declarations for the pushout side.  MPC produced environment size 11,045, giving binary pullback and pushout map-composition coverage without exposing a new checker rule.

`CategoryTheory.Limits.kernelComparison_comp_ι` accepts as the first kernel-comparison probe.  Building `Mathlib.CategoryTheory.Limits.Shapes.Kernels` added zero objects, equalizers, image declarations, zero morphisms, and kernel support.  MPC reused 434 declaration entries, checked 81 new entries, and produced environment size 11,329.

`CategoryTheory.Limits.π_comp_cokernelComparison` accepts as the cokernel-side companion.  The target reuses most of the kernel-comparison cache and checks the dual projection equation for cokernels under a functor preserving zero morphisms.  MPC reused 477 declaration entries, checked 55 new entries, and produced environment size 11,384.

`CategoryTheory.Limits.Pi.map_comp_map` accepts as a product-family category probe.  The target checks the dependent product-family version of the map-composition pattern after the binary pullback and pushout probes.  MPC reused 430 declaration entries, checked 58 new entries, and produced environment size 11,109.

`CategoryTheory.Limits.Sigma.map_comp_map` accepts as the coproduct-family companion.  The target reuses most of the product-family cache and checks the dual map-composition pattern over coproduct families.  MPC reused 462 declaration entries, checked 37 new entries, and produced environment size 11,148.

The measure-theory probe found a cache-adapter bug before it reached a checker boundary.  `Measurable.dist` from `Mathlib.MeasureTheory.Constructions.BorelSpace.Metric` produced an 834,810-row artifact, and the first cached stats run failed because requested-content lookup materialized all requested keys into one SQLite temporary table.  The adapter now queries requested content keys in bounded direct chunks and preserves SQLite stderr on write failures; this changes checked-layer storage behavior without changing MPC checking.

After the cache fix, the same probe reached `Rat.addCommGroup._proof_1` at declaration index 3277.  The isolated root `Rat.addCommGroup._proof_1` from `Mathlib.Data.Rat.Lemmas` reproduces the wall at index 1826, with 95 type nodes and 2,575 value nodes in the selected theorem.  Disabling the equality-rec endpoint fallback as a diagnostic did not advance the proof, and direct reduction of projection constants did not advance it either.  The projection-constant reducer remains a projection-package correction for exported structure accessors, while the rational proof is classified as a performance boundary over ordinary equality transports rather than a new rule-package conclusion.

`Nat.primeFactorsList_unique` reaches a performance boundary before the requested theorem.  Cache stats replay reused the prefix through declaration index 2717 in 6,045 ms, then timed out at index 2718, `_private.Mathlib.Data.Nat.Sqrt.0.Nat.sqrt_isSqrt`.  The hard theorem has 9 type nodes and 47,233 value nodes, with applied heads dominated by `OfNat.ofNat`, `HAdd.hAdd`, `HDiv.hDiv`, `Nat.log2`, `HShiftLeft.hShiftLeft`, `HMul.hMul`, and `Nat.sqrt`, so this probe currently classifies as ordinary numeric proof-conversion throughput rather than a new rule-package gap.

`CategoryTheory.Abelian.image_ι_comp_eq_zero` reached a host-resource boundary during the first abelian-category probe.  The first attempt used the unqualified root `CategoryTheory.image_ι_comp_eq_zero`, and the probe driver rejected the resulting metadata-only export before invoking MPC.  The corrected root built and exported a 27 MB artifact, but the checker process was killed with exit code 137 before it produced declaration stats or a checker rejection.  The workspace had 4.0 GB free and `/tmp` had 812 MB free after the kill, so the run did not fail from disk exhaustion.  Kernel logs were not readable from the sandbox, so the exact kill reason remains unconfirmed.
